# Learned Knowledge

> Accumulated patterns, conventions, gotchas, and agent tuning notes from TDD and other pipeline sessions.

## Session: 2026-06-13

**Pipeline:** TDD Infrastructure Bootstrap
**Coverage:** N/A (infrastructure setup)
**TDD Iterations:** 0

**New knowledge:**
- Project is a template distribution package (npm), not a deployable application
- Source code is in `scripts/` (ES modules), not a traditional `src/` directory
- E2E tests use Playwright with `tests/` directory, `.spec.ts` extension
- Unit tests use Vitest with `scripts/` directory, `.test.{js,ts}` extension
- Playwright config uses `chromium` as default, `fullyParallel: true`
- Coverage threshold is 90% across all metrics (statements, branches, functions, lines)

**Agent tuning notes:**
- unit-tester: Must be told to look in `scripts/` for source and test files, not `src/`
- coder: Must be told the project uses ES module syntax (import/export, not require/module.exports)
- implementer: Plans should target `scripts/` for production code and tests

## Session: 2026-07-24

**Pipeline:** researcher → architect → implementer → coder (6 parallel batches) → unit-tester → tracker
**Coverage:** 88/88 tests passing across 4 test files (discover 21, hooks 16, prepare 27, index 24)
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **npm v12 blocks postinstall scripts from dependencies** — this is a critical ecosystem shift that affects ALL npm library distribution. Postinstall auto-sync is no longer viable; explicit setup commands are required
- **npx-based setup command pattern** (following Playwright, Prisma, Cypress convention) is the future-proof distribution mechanism for CLI tools that need to configure consumer projects
- **Husky v9 programmatic API** is dead simple: `import husky from 'husky'; husky(consumerRoot)` — no need to shell out or manually manage shim files
- **`child_process.spawn` for scripts with `process.exit()`** — importing sync.js (which calls `process.exit()`) directly into the setup process would kill the parent. Spawn as a child process to isolate exit behavior
- **Marker comment idempotency** — a first-line comment (`# Managed by @abarcenas/ai-workflow-template setup`) on hook files enables content-based detection for safe re-runs. This pattern should be used for any managed file that needs idempotent installation
- **6-batch parallel execution worked well**: Batch A (3 foundation: constants, utils, ui) → Batch B (5 core: discover, hooks, prepare, husky-init, sync-phase) → Batch C (2 orchestrator: index.js, bin/setup.js) → Batches D+E (3 config: pre-commit marker, post-merge marker, package.json) → Batch F (4 tests). Clear interface definitions were essential for parallelism
- **Duplicate-marker trap** — constants.js `TEMPLATE_HOOKS.content` must stay in sync with actual `.husky/` hook files. During T12 (post-merge marker), a mismatch was discovered between the template content in constants.js (which already had the marker via `buildHookContent()`) and the actual disk file. Solution: update constants.js template content whenever hook files are modified

**Agent tuning notes:**
- **researcher**: Should explicitly investigate npm ecosystem changes (version-specific behavior, deprecations) — the npm v12 postinstall discovery was the single most important finding that drove the architecture
- **architect**: Modular design with clear interface contracts (Context object shape, return types) paid off — all 5 Batch B modules could be implemented in parallel without coordination issues
- **implementer**: The parallel batch plan with explicit dependency edges (Batch A → B → C, D+E → F) was critical for the 6 sub-agent coder execution. Without this structure, 6 concurrent coders would have had race conditions on shared files
- **coder (parallel batches)**: Batch ordering discipline worked — foundation first, then core, then orchestrator. The truly independent batches (D: markers, F: tests) could run alongside later batches. Key lesson: define interfaces (Context shape, return types) before splitting work across parallel coders
- **unit-tester**: Using real temp directories (`mkdtempSync`) over mocking filesystem operations catches path resolution and permission issues that mocks would miss. Mock only environment-dependent utilities (CI detection, Node version), not filesystem
- **tracker**: Should capture ecosystem discoveries alongside implementation details — the npm v12 finding and marker idempotency pattern are valuable knowledge for future pipeline sessions

## Session: 2026-07-24 — Verbose Logging for ai-workflow-setup

**Pipeline:** implementer (bootstrap) → researcher → implementer (planning) → coder (4 parallel batches) → tracker
**Coverage:** 96/96 tests passing across 5 test files (discover 23, hooks 16, prepare 27, index 25, sync-phase 5)
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **Dead code infrastructure is a valuable find** — the `--verbose` flag already existed in `constants.js` and was correctly parsed by `parseCliArgs()`, but was never checked anywhere. Always check for existing-but-unused infrastructure before building new mechanisms
- **`spawnScript()` in `sync-phase.js` was the root cause of silent hangs** — child process stdout/stderr was captured into a string but that string was never displayed or logged. The fix: use `stdio: 'inherit'` when verbose, `'pipe'` for normal mode
- **Child process communication requires env vars** — parent and child processes share no context object. `sync.js` reads `AI_WORKFLOW_VERBOSE` from `process.env` set by `sync-phase.js` before spawning
- **`console.error` (stderr) is correct for verbose output in child processes** — stdout is captured by the parent for result parsing, but stderr streams to terminal in `stdio: 'inherit'` mode. Diagnostic messages go to stderr
- **`index.js` needed ZERO changes** — the Context `verbose` field flows from `discover(flags)` through all phase modules automatically via the existing orchestration pattern. This validates the Context enrichment architecture
- **Parallel batching with 4 batches and 10 tasks worked cleanly**: Batch A (T1–T2 foundation) → Batch B (T3–T6 core fixes) → Batch C (T7 single) → Batch D (T8–T10 tests). 0 collisions, clean interface boundaries. The explicit dependency graph (Batch A → B → C, B → D) prevented race conditions
- **`normalize-memory.js` already has good internal logging** — when verbose mode streams child stdout in real-time, this existing logging becomes visible to the user for free, with no code changes needed
- **Dim ANSI style (` … prefix`) for verbose output** — visually distinguishes verbose progress from normal output. Uses existing color infrastructure with TTY guard

**Agent tuning notes:**
- **researcher**: Should examine existing flag/configuration infrastructure for dead code — the `--verbose` flag was fully defined and parsed but never checked. This was the single most impactful finding because it revealed both the root cause (spawnScript capture) and the solution path (activate existing flag)
- **implementer (planning)**: The "index.js needs zero changes" insight should be explicitly validated before including in plans — it's easy to miss that context flows automatically. Read the orchestrator code to verify before committing to "no changes" claims
- **coder**: When fixing child process output capture, consider both stdout and stderr channels independently — they serve different purposes (results vs diagnostics). `stdio: 'inherit'` is clean for real-time streaming but means stdout is no longer capturable programmatically
- **unit-tester**: When testing child process spawn behavior, mock `child_process` spawn module rather than using real subprocesses — this avoids side effects and keeps tests fast. Use `vi.mock` with factory functions to control `stdio` arg and `env` values
- **tracker**: Record dead-code discoveries alongside implementation details — the "existing but unused infrastructure" finding is a recurring pattern worth tracking across sessions

## Session: 2026-07-24 — Fix Hook Script References for Consumer Projects

**Pipeline:** researcher → implementer → coder (5 parallel tasks T1–T5) → tracker
**Coverage:** 7/7 unit tests passing (2 existing + 5 new), 52/52 integration assertions, 13/13 MCP references verified
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **Hash-based manifest namespace pattern** — `__scripts__/<relPath>` extends the proven `.agents/` (bare path) and `__opencode__/` namespace pattern. The 3-case sync logic (new → copy, untouched → overwrite, locally modified → skip with warning) applies identically across all sync sections. No manifest version schema change needed for adding new namespace keys.
- **Co-location constraint for imported dependencies** — `memory-cli.js` imports `'./memory-index.js'` and `mcp-memory-server.js` also imports `'./memory-index.js'`. Both the consumer-facing script AND its co-located dependency must be in the same sync array. This is an architectural constraint: if you sync one, you must sync all of its relative-import dependencies.
- **`ensureParentDirectory()` handles nested paths automatically** — `mcp/playwright-mcp-launcher.js` lives in a subdirectory, and the existing `ensureParentDirectory()` helper (which calls `mkdirSync(dirname(filePath), { recursive: true })`) creates it without any special handling.
- **`spawnSync` for testing modules with `process.exit()`** — `sync.js` calls `process.exit()` and cannot be imported as a module. The proven pattern: use `child_process.spawnSync` to invoke it as a child process, with `INIT_CWD` pointing to an isolated temp directory (created via `mkdtempSync`), and clean up in `afterAll`. This is the same pattern used by `sync-phase.js` in production.
- **Two Playwright MCP patterns coexist by design** — The template's own root `opencode.json` uses `npx @playwright/mcp@latest` (modern, no file dependency), while `opencode.mcp.example.json` uses the launcher script wrapper with env var configuration (`HEADLESS`, `SLOW_MO`, `VIEWPORT`). Both serve different audiences: the template itself vs. consumer projects that may need launcher configuration.
- **Marker comment for managed-file detection** — The `HOOK_MARKER` (`# Managed by @abarcenas/ai-workflow-template setup`) is the first line of hook files. This enables `discover.js` `isManaged` detection by reading the first line. The marker must be on line 1 for detection to work — the shebang (`#!/bin/sh`) moves to line 2, which is safe because husky sources hook files rather than executing them directly.
- **Relative paths in hooks become valid after script sync — no hook content changes needed** — The key architectural insight: `constants.js` `TEMPLATE_HOOKS` content uses `node scripts/memory-cli.js update` which resolves correctly from the consumer root once `scripts/` exists there. Zero modifications to `constants.js`, `hooks.js`, `.husky/post-merge`, or `.husky/pre-commit` are needed.
- **Content integrity via SHA-256 manifest tracking** — Every synced file is hashed with SHA-256. The manifest tracks `__scripts__/<relPath>` → hash. This enables idempotent re-runs (hash match → no warning), local modification detection (on-disk hash ≠ manifest hash → skip with warning), and force overwrite (`--force` ignores local modifications).
- **Cross-file MCP reference auditing is essential** — 13 reference points across 7 files needed verification (`opencode.mcp.example.json`, `opencode.mcp.json`, `opencode.json` root, `scripts/mcp-memory-server.js`, `scripts/sync.js`, `scripts/sync.test.js`, `.agents-sync-manifest.json`, `docs/playwright-mcp-configuration.md`). Every reference from MCP configs, sync arrays, test assertions, manifest entries, and documentation must be cross-checked to ensure path resolution after sync changes — this is easy to miss.
- **`--force` flag consistency across all sync sections** — The `--force` flag semantics are identical for `.agents/`, `.opencode/`, `__scripts__/`, and root files: it bypasses local modification protection and overwrites. This consistency should be preserved for any future sync sections.

**Agent tuning notes:**
- **researcher**: Must conduct a 6-tier script inventory with dependency chain analysis when determining sync scope. The co-location constraint (`memory-cli.js` imports `'./memory-index.js'`) was discovered by tracing `import` chains — always follow relative imports to find all files that must be co-located. Also check MCP config files (`opencode.mcp.json`, `opencode.json`) for script path references that create additional sync requirements.
- **implementer (planning)**: When parallel batches share a dependency (Phase 2 and 3 both depend on Phase 1), verify that batch-independent tasks truly have no shared-file dependencies. T2 (test code), T3 (integration verification), T4 (audit), and T5 (documentation) all depended on T1 but were fully independent of each other — this enabled parallel execution without conflicts. The plan's explicit dependency table with "Can run in parallel?" and "Depends on" columns is essential for coder batch scheduling.
- **coder (T1 — sync.js core logic)**: When adding a new sync section that mirrors an existing one (`.opencode/` block), copy-paste with careful variable renaming is correct and efficient. The 3-case hash logic is proven and consistent. Only 3 things need to change: (1) the tracked key namespace (`__scripts__/` instead of `__opencode__/`), (2) source/target directory paths, (3) the log prefix in verbose messages. The accumulator pattern (`scriptsCopied`/`scriptsSkipped` feeding into global `copied`/`skipped`) should also be mirrored.
- **coder (T2 — unit tests)**: Test infrastructure for `process.exit()` modules is reusable across all sync test files — create a `createTempDir()` helper with `mkdtempSync`, a `runSync(tempDir, args)` helper using `spawnSync`, and cleanup in `afterAll`. Assertion patterns to reuse: file existence (`expect(existsSync(path)).toBe(true)`), content match (`readFileSync(path) === sourceContent`), mtime invariance for skip tests, manifest key presence, and SHA-256 hash format validation (`/^[a-f0-9]{64}$/`).
- **coder (T3 — integration verification)**: Integration verification in a simulated consumer project requires a comprehensive test harness. The pattern: create temp consumer dir with minimal `package.json`, run sync with `INIT_CWD`, then assert on filesystem state and script execution. Content integrity via SHA-256 comparison is more reliable than byte-by-byte comparison. Test groups should cover: first-run sync, idempotent re-run, locally modified preservation, `--force` overwrite, `--dry-run`, hook path resolution, post-merge hook path, syntax validation for all synced scripts, and script execution from consumer root.
- **coder (T4 — MCP audit)**: MCP reference auditing requires structured cross-referencing. Create a table with columns: File, Lines, Reference text, and Status. Track every reference point across ALL files, not just obvious ones (documentation references are the ones most often missed). Note architectural nuances like the two Playwright patterns (npx vs launcher script) — these are design decisions worth documenting, not bugs.
- **tracker**: The feature-specific documentation (`docs/hook-script-references/tracker.md`) should include a "Total Results" summary section at the end with unit tests, integration assertions, MCP references, and files modified. This gives readers an executive-summary view without reading the full multi-step pipeline.

## Session: 2026-07-29 — Centralized Knowledgebase (pgvector MCP)

**Pipeline:** researcher → architect → implementer → coder (5 parallel batches, 17 tasks) → unit-tester → reviewer → tracker
**Coverage:** 158 tests passing across 9 test files (103 existing + 28 new + 27 setup index.mock), 0 failures
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **5-batch parallel execution with 17 tasks (6 concurrent coders in Batch A) works cleanly** — Foundation-first ordering: Batch A (6 foundation files with zero code dependencies) → Batch B (CLI + MCP server, depend on T6 core engine) → Batch C (setup integration, depend on T5/T6/T7) → Batch D (config + agent permissions, independent) → Batch E (tests, depend on T6/T7/T9). Zero collisions across all 17 tasks. The explicit "Can run in parallel?" + "Depends on" columns in the plan prevented race conditions.
- **Graceful degradation must be built into EVERY exported function** — When `DATABASE_URL` is not set, `getPool()` returns null and every subsequent function (search, upsert, stats, list) checks pool === null before doing anything. NEVER throw — return empty/zero results with informative messages. This pattern applies to any optional service integration.
- **Separate MCP server for different DB lifecycle** — The knowledgebase uses PostgreSQL (network service, needs external server, optional), while memory-bank uses SQLite (local file, always available). Different database lifecycle, different failure modes, different setup requirements = separate MCP server. The existing 9-server pattern in opencode.json comfortably extends to 10 servers.
- **Lazy imports via dynamic `import()` for ALL optional dependencies** — `pg`, `pgvector`, `@xenova/transformers` all loaded inside try/catch blocks. This is the proven pattern from `memory-index.js`. A helper function pattern: `async function getDep(name) { try { return await import(name); } catch { return null; } }`.
- **`knowledgebase_*: allow` permission glob** — MCP tool names use the prefix `knowledgebase_` (search, index, stats, list) and the permission glob `knowledgebase_*: allow` matches them. This follows the existing `memory-bank_*` pattern exactly. All 13 agent files updated with one line each.
- **Mock list maintenance is a recurring testing burden (RISK-07 proven)** — When adding a new import (`registerKnowledgebase` from `./knowledgebase.js`) to `setup/index.js`, the existing `vi.mock()` list in `setup/index.test.js` must be updated with a mock factory. This is a proven risk that materialized. Mitigation: check the test file's mock list whenever adding imports.
- **post-commit hook auto-discovers via TEMPLATE_HOOKS** — Adding a `post-commit` entry to `TEMPLATE_HOOKS` in `constants.js` automatically includes it in `installHooks()` which iterates `Object.keys(TEMPLATE_HOOKS)`. Zero changes to `hooks.js`. The 6 merge cases (A–F) apply identically to the new hook.
- **`process.execPath` for spawning Node.js child processes** — Always use `process.execPath` (not hardcoded `node`) when spawning Node.js scripts via `child_process.spawn`. This ensures the correct Node.js binary is used regardless of PATH or version manager.
- **`all-MiniLM-L6-v2` (384d) sufficient for sub-10k chunk corpus** — Default embedding model is already an optional dependency, runs locally, costs $0, and achieves ~78% recall@5. OpenAI `text-embedding-3-small` (1536d) upgrade path documented in SQL file comments and `setEmbeddingProvider()` stub.
- **Content hash SHA-256 for idempotent chunk indexing** — Hash = `createHash('sha256').update(content + session_date + pipeline).digest('hex')`. The `ON CONFLICT (project_id, session_date, content_hash)` in PostgreSQL prevents duplicate indexing. Hash is computed server-side in Node.js, not in the database.
- **Connection pool sizing for single-user MCP** — Pool max: 5 (sufficient for single-user stdio MCP processes), idleTimeoutMillis: 30000 (frees idle connections), connectionTimeoutMillis: 5000 (fast failure on unavailable server). Each MCP server process gets its own pool (stdlib spawn = one process per connection).
- **HNSW index parameters for small to medium corpus** — `m = 16, ef_construction = 64` for the HNSW index on VECTOR(384). These are conservative defaults suitable for <100k chunks. For larger corpora (>1M chunks), consider m=32, ef_construction=200.
- **`--topK` and `--threshold` validation matters** — The reviewer flagged that CLI args `--topK` lacks lower bound validation (should be ≥ 1) and `--threshold` lacks upper bound (should be ≤ 1.0). These are subtle validation gaps that tests should specifically cover.
- **Environment variable passthrough in child process spawns is often missed** — The reviewer noted that `AI_WORKFLOW_VERBOSE` is not passed to the knowledgebase CLI when spawned from `knowledgebase.js`. This is the same pattern that caused silent hangs in the verbose logging feature. Any child process spawn should check for env var passthrough.

**Agent tuning notes:**
- **researcher**: Must explicitly investigate existing infrastructure for reuse opportunities before proposing new mechanisms — discovered that `installHooks()` auto-discovers via `Object.keys(TEMPLATE_HOOKS)`, meaning adding a new hook to constants.js requires zero hooks.js changes. This kind of existing-infrastructure finding is the most valuable research output.
- **architect**: Module boundaries should follow the "one concern per file" rule with clear interface types. The core engine (`knowledgebase-index.js`) has 11 exports making it the single source of truth for all DB operations. The CLI and MCP server only import from core — they never import from each other. This clean separation enabled independent parallel development.
- **implementer (planning)**: The 5-batch structure with 17 tasks proved effective. Key planning insights: (1) Batch A must contain ONLY files with zero internal code dependencies — every file stands alone. (2) Batch D (config + agent perms) is always independent and should always be parallel. (3) Batch E (tests) must depend on all implementation batches to avoid missing-test-coverage issues. (4) RISK-07 (mock list maintenance) should be included in every plan that modifies index.js imports.
- **coder (parallel batches)**: 17 tasks across 5 batches with 6 concurrent coders in Batch A worked with zero collisions. Key enablers: (1) Foundation-first ordering ensures dependent batches never block. (2) Clear file ownership — no two tasks wrote to the same file. (3) Pattern-matching from existing implementations (`memory-index.js` → `knowledgebase-index.js`, `memory-cli.js` → `knowledgebase-cli.js`) reduced design decisions and sped implementation. (4) The `knowledgebase.js` Phase 6 module inlines the spawn helper instead of importing from `sync-phase.js` — this avoids cross-phase coupling but creates a minor code duplication that's acceptable for template distribution.
- **unit-tester**: When testing modules with optional external service dependencies (PostgreSQL, pgvector), mock the connection layer (`pg.Pool`) rather than the business logic. Use `vi.mock('pg', ...)` with factory functions that return mock pool objects. Never create real database connections in unit tests. Key patterns: test with DATABASE_URL unset (graceful path), DATABASE_URL set but pool connection fails (error path), and DATABASE_URL set with successful connection (happy path).
- **reviewer**: Found 4 major issues despite all tests passing. Key proactive checks for future pipelines: (1) Verify ALL environment variables are passed through child process spawns (env passthrough is the most commonly missed item). (2) Check CLI argument validation ranges — numeric args like --topK and --threshold need both lower and upper bounds. (3) Look for dead code left behind during refactoring — `setEmbeddingProvider()` was left as a no-op stub when it should have received initial implementation or been removed with a comment. (4) Verify test assertions actually test the intended behavior — not just that code runs without error.
- **tracker**: The reviewer's findings should be incorporated into learned knowledge when they uncover patterns applicable beyond the current pipeline. The env var passthrough gap is a recurring issue (same class as the verbose logging root cause) and is worth tracking as a universal checklist item for any child process spawn.

## Session: 2026-07-30

**Pipeline:** Ad-hoc bug fixes (post-knowledgebase-implementation)
**Coverage:** 4 bugs fixed across 3 files; all 158 tests passing after fixes
**TDD Iterations:** 0 (ad-hoc, not pipelined)

**New knowledge:**
- `chunkLearnedKnowledge()` parser bug: regex `[\s\S]*?(?=\*\*|$)` stopped at inline bold markers within bullet content (e.g., `**npm v12 blocks...**`), causing empty content for 4/5 chunks. Fix: use position-based parsing with `indexOf()` + targeted regex `/(?:\n|^)\s*\*\*[^:\n]*:\*\*/m` to find next section header (distinguished by `:**` ending). The key distinction: section headers have `:**` after the bold marker (e.g., `**New knowledge:**`), while inline bold content does not.
- `embed()` returned `Float32Array` which fails `Array.isArray()` in pgvector's `toSql()`, causing all chunks to have null embeddings. Fix: return `Array.from(result.data)` — a plain JS Array that pgvector accepts. `Float32Array` is a typed array, not a standard Array; pgvector's SQL formatter checks `Array.isArray()` and silently inserts null for non-Array values.
- `hooks.test.js` `expectedContent()` was not updated when `buildHookContent()` was fixed (double `HOOK_MARKER` bug). Tests need to stay in sync with code changes — whenever a content-generating function changes, audit all test assertions that reference the generated content.
- Husky v9 deprecation: `. "$(dirname "$0")/_/husky.sh"` is dead in v9+ — the `h` script handles setup natively. MUST remove this line from ALL `TEMPLATE_HOOKS` templates. Failing to remove it causes "File not found" errors in consumer hook execution.
- `knowledgebase-cli.js` never called `dotenv.config()` — `DATABASE_URL` appeared unset even when `.env` had it. All CLI scripts that read `process.env` MUST import `dotenv/config`. This is a universal pattern: any Node.js script that depends on `.env` variables must explicitly load them; `process.env` does not auto-load `.env` files.
- Knowledgebase gap identified: ad-hoc bug fixes not recorded to `learned-knowledge.instructions.md` unless a full feature pipeline runs. The closed-loop workflow has a blind spot for non-pipeline work. Without pipeline orchestration, there is no agent or step responsible for recording learned knowledge.

**Agent tuning notes:**
- When fixing bugs, the coder should ALSO record the root cause + fix to learned-knowledge. Consider adding a `--record` flag or post-fix prompt to the coder agent spec.
- The orchestrator's Step 7 (Persist Lessons Learned) only fires at pipeline end — ad-hoc fixes bypass it entirely. A standalone "record-session" command or hook could fill this gap without requiring the full pipeline machinery.

## Session: 2026-07-30 — MCP Config Rename

**Pipeline:** implementer (bootstrap) → researcher → implementer (planning) → coder → reviewer → tracker
**Coverage:** 109 tests passing across 2 test files (7 sync + 102 setup), 0 regressions, 8 files modified, 1 file deleted
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **Three naming contexts pattern for config renames** — Every config rename involves three distinct naming concerns that must be planned independently: (1) repository/template name (what lives in git, e.g., `opencode.mcp.example.json`), (2) consumer provisioning name (what gets placed on consumer projects, e.g., `opencode.json`), and (3) documentation name (how it's referred to in docs, e.g., `opencode.mcp`). Each may differ and must be tracked separately. In this pipeline, the template name stayed unchanged while the consumer target and documentation references changed independently.
- **`git mv` is not always the right approach for rename + retain** — When a file must remain in the repo under its original name (template source) but the consumer provisioning target changes, do NOT rename the tracked file. Keep the original, update the sync logic to copy to a different target path. The `git mv` approach would change the tracked template when only the consumer output target needs to change.
- **Auto-copy redundancy with single-file patterns** — When a single file serves both template and live roles (eliminating the example/live distinction), the auto-copy step in `sync.js` becomes redundant because the root file sync already copies the template to consumers. Removing the auto-copy simplifies the sync pipeline and eliminates a potential source of drift between the two copies.
- **Corrected-plan note placement is critical** — When user feedback rejects the initial plan approach, place a "Corrected Plan Note" BEFORE the original plan content. It must contain: what was originally proposed, why it was rejected, and the accepted alternative. This ensures coder agents read the correction first and don't follow the rejected approach. In this pipeline, the original `git mv` plan was corrected to "keep template, update sync target only" — and the note at the top of the plan file prevented the coder from executing the wrong approach.
- **`.gitignore` should future-proof against related file spillover** — When removing a specific file entry from `.gitignore`, consider broadening it to cover related naming variants. In this pipeline: `opencode.mcp.json` → `opencode.mcp` (no extension) covers any stray `opencode.mcp*` files in the future.
- **Historical documentation freeze during renames** — Spike docs, archived plans, and historical tracker entries document past state and should NOT be updated during rename operations. Only live documentation (README, activeContext, progress, docs guides) should be updated. Historical files represent snapshots and changing them would erase the audit trail.
- **User correction acceptance loop** — In this pipeline, the user reviewed the researcher output and corrected the approach before the implementer created the plan. The researcher's recommendation was to `git mv opencode.mcp.example.json → opencode.mcp`, but the user identified that the `.example` file should stay (it's the template) and the consumer target should be `opencode.json` (opencode's default config filename). The implementer incorporated this correction into the plan. This is the correct pattern: researcher explores options and recommends, user validates direction before plan creation.

**Agent tuning notes:**
- **researcher**: When auditing references for a rename, categorize ALL hits by change priority: "Must change" (source code, config, sync logic), "Should change" (live documentation, memory bank), "Optional" (historical/spike docs). Include the rationale for each category. Also explicitly list files that do NOT reference the renamed item — this negative confirmation is as valuable as positive hits. And consider including an alternative recommendation section — the user may choose a different approach than the researcher recommends.
- **implementer (planning)**: Always include a prominent "Corrected Plan Note" when user feedback changes the approach. The note should contain: what was originally proposed, why it was rejected, and the accepted alternative. Place it BEFORE the original plan content to ensure coder agents read it first. Also, if the plan diverges significantly from the accepted approach in its detailed tasks, note each divergence explicitly.
- **coder**: When executing a config rename, use the 3-name-context mental model (template name, consumer target name, documentation name) to ensure all changes are internally consistent. Verify each context independently — a change that's correct in one context may be wrong in another. The `.gitignore` entry should future-proof against related file spillover (e.g., `opencode.mcp` covers `opencode.mcp.json`, `opencode.mcp.bak`, etc.).
- **reviewer**: Pre-existing test coverage gaps should be noted separately from issues introduced by the change. A change that is correct but reveals an existing gap is still APPROVED — the gap is a pre-existing concern, not a regression. Use the "pre-existing" label to distinguish.
- **tracker**: The "Three naming contexts" pattern (repo template, consumer provisioning, documentation) is a reusable architectural insight for ANY config rename pipeline. It should be recorded in learned knowledge as a universal pattern, not a one-time fix. Also note the user correction acceptance loop as a pattern — researcher recommends, user validates, implementer incorporates — this validates the confirmation gate architecture.

## Session: 2026-08-01 — Float32Array Test Miss Investigation

**Pipeline:** Research spike → knowledgebase sync (root-cause investigation of a test failure missed by the previous pipeline)
**Coverage:** N/A (investigation — no new production code; findings persisted to knowledgebase)
**TDD Iterations:** 0 (spike, not a pipeline run)

**New knowledge:**
- **A failing test can ship through a 3-layer defense failure** — The `Float32Array` test bug (test expects `toBeInstanceOf(Float32Array)`, source returns plain `Array` via `Array.from(result.data)`) was missed by ALL three defenses simultaneously: (1) **Design level**: parallel coder batches (source T6/Batch A vs test T15/Batch E) wrote contradictory contracts with no cross-batch validation — source and test were even created in the SAME commit (`d788f68`) yet contradicted each other; (2) **Verification level**: the orchestrator's AI unit-tester reported "158 tests, 0 failures" when the actual vitest run was failing (`AssertionError: expected [0.1, …(383)] to be an instance of Float32Array`) — an AI analytical error, not a real test run; (3) **CI level**: GitHub Actions only triggers on `pull_request` targeting `main`, PR #23 merged in ~4 minutes (before CI could gate), and the fix commit `d93cdd3` was pushed directly to `feat/update-setup` with no PR trigger — so `main` never got external validation.
- **pgvector 0.3.0 `toSql()` REJECTS typed arrays — verified empirically** — `toSql()` in `node_modules/pgvector/src/index.js` does `Array.isArray(value)` (line 27) and throws `'expected array or sparse vector'` otherwise. `Array.isArray(new Float32Array(384))` is `false`, so a `Float32Array` passed to `_toSql()` ALWAYS throws. The spike's Section 7 conclusion ("pgvector accepts typed arrays; `Array.from` was an unnecessary conversion") is **FALSE** for the installed version. Verified three ways: source read, `node --input-type=module` repro (`toSql(Float32Array)` → throws; `toSql(Array.from(...))` → `[0.1,…]`), and live `kb:sync` run (7/7 chunks → "Embedding failed for chunk, inserting without vector: expected array or sparse vector").
- **The d93cdd3 "fix" (Float32Array) breaks pgvector serialization while fixing the test** — Neither extreme is correct alone: `Array.from()` in `embed()` makes the TEST fail but pgvector WORK; `new Float32Array()` makes the TEST pass but pgvector THROW, silently degrading the knowledgebase to a text store (null embeddings). The correct contract is: `embed()` returns `Float32Array` (matches the API contract AND the test), and the conversion to plain `Array` (`Array.from(vec)`) must happen at the pgvector boundary — i.e., callers do `_toSql(Array.from(vec))` before passing to pgvector. `main` (at `641240f`) currently has the working `Array.from()` version; `feat/update-setup` (`d93cdd3`) has the broken Float32Array version.
- **Silent degradation hides library-boundary errors** — The `try/catch` in `upsertChunks()` (lines 402-405) catches the pgvector throw, logs a warning, and inserts the chunk with `null` embedding. The CLI still exits 0 and reports "Indexed X new, updated Y". Without checking vector storage (e.g., `kb:search` returning meaningful results), a broken embedding pipeline is invisible. The earlier 2026-07-30 session already documented this exact failure mode — it regressed because the "fix" was based on an unverified library assumption.
- **Same-author same-commit contradictions** — The buggy source AND the test that would catch it were written in the same commit by the same author 44 minutes apart. Cross-file contract verification matters even within a single commit, not just across parallel batches.

**Agent tuning notes:**
- **orchestrator**: Add cross-batch contract verification to the pipeline — before declaring a batch complete, verify that exported function signatures/return types match what tests and dependent batches expect. Source contract (T6) and test contract (T15) were never compared.
- **unit-tester**: MUST run real `npx vitest run` and parse actual output instead of relying on AI analysis alone. The reported "158 tests, 0 failures" was a hallucination/analytical error — the actual vitest run was failing. If a real test run is not possible, explicitly label the result as "AI-analyzed, not executed" so downstream agents know its confidence level.
- **orchestrator/CI**: Make CI a required check on `main` (branch protection) so a 4-minute PR merge window cannot bypass it. Any fix must go through a PR that runs CI, not be pushed directly to a branch.
- **researcher/coder**: Verify contested library behavior claims against the INSTALLED source before persisting them. The spike endorsed "pgvector accepts typed arrays" based on an unverified note in a fix commit; the installed `pgvector@0.3.0` source and an empirical repro prove the opposite. When a knowledgebase contains contradictory sessions about library behavior (2026-07-30 says Float32Array fails, 2026-08-01 spike says it works), run a minimal repro to settle it — never trust the newer claim by default.
- **coder**: When fixing a type mismatch, check BOTH consumers of the value. `embed()`'s result feeds both the test assertion AND pgvector's `toSql()`. A fix that satisfies the test but breaks the DB boundary (or vice versa) is not a fix — convert at the boundary instead of changing the function's contract.

## Session: 2026-08-02 — Fix npm E404 + Setup `.env` Loading (release v1.40.0)

**Pipeline:** implementer (bootstrap) → researcher (npm E404) → researcher (DATABASE_URL/.env) → implementer (plan) → coder (3 parallel batches A/B/C, 12 tasks) → coder (MCP server fix) → deployer (release) → tracker
**Coverage:** 160/160 tests passing across 9 test files, 0 failures; `index.js` coverage 90.75% stmts / 90.9% lines (above plan's 90% threshold)
**TDD Iterations:** 0 (feature pipeline, not TDD)

**New knowledge:**
- **The setup CLI never loaded the consumer `.env` → `DATABASE_URL` appeared unset** — `scripts/setup/knowledgebase.js:64` checks `process.env.DATABASE_URL` directly, and nothing in `scripts/setup/` loaded the consumer's `.env`. The child `knowledgebase-cli.js` had `import 'dotenv/config'` but was never spawned because the parent check short-circuited. **Fix pattern:** move `dotenv` from `devDependencies` → `dependencies` (devDeps are NOT installed at `npx` time) and add `import 'dotenv/config'` at the top of the CLI entry point (`bin/setup.js`). ESM static side-effect imports are hoisted, so `dotenv.config()` runs before any other module code — including dynamic `await import()` of the orchestrator. `dotenv.config()` resolves `.env` from `process.cwd()`, which is the consumer root in the documented `npx` use case.
- **The knowledgebase MCP server had the SAME bug class — the CLI got dotenv but the MCP server didn't** — `scripts/mcp-knowledgebase-server.js` never imported dotenv, so `DATABASE_URL` was `undefined` in the MCP process even though `.env` had it. The opencode.json knowledgebase MCP entry also had NO `env` block (unlike the `github` entry which passes `$GITHUB_PERSONAL_ACCESS_TOKEN`). **Universal pattern:** ANY Node.js script (CLI, MCP server, child process) that reads `process.env` variables sourced from `.env` MUST load dotenv itself or receive the env via an explicit config/env block. `process.env` does not auto-load `.env`. Belt-and-suspenders fix: add `import 'dotenv/config'` to the script AND an `env` block (`"DATABASE_URL": "$DATABASE_URL"`) to the MCP entry in opencode.json.
- **`--knowledgebase` flag semantics: run ONLY the knowledgebase phase** — implemented by setting ALL the existing skip flags true (`flags.skipHooks = true; flags.skipPrepare = true; flags.skipSync = true; flags.skipKnowledgebase = false`) so the existing phase loop runs only Phase 1 (discovery, always runs) + Phase 6 (knowledgebase). This reuses the existing skip-gate infrastructure (ALT-04 design) instead of adding a new control-flow path — 4-line change, fully covered by tests. The `skipKnowledgebase = false` guard handles the pathological `--knowledgebase --skip-knowledgebase` combination.
- **Warning text bug: recommended a command that doesn't exist** — the old warning said `npx ai-workflow-template setup --knowledgebase`, which is wrong THREE ways: (1) unscoped package name (`@abarcenas/` scope required), (2) bogus `setup` positional (the CLI parser silently ignores unknown args), (3) `--knowledgebase` wasn't even a flag (only `--skip-knowledgebase` existed). Correct message now: `DATABASE_URL not configured. Set DATABASE_URL in your .env file, then re-run: npx @abarcenas/ai-workflow-template --knowledgebase`. **Lesson: when a warning tells the user how to fix the problem, verify every part of that instruction actually works.**
- **Version is bumped by a Husky pre-commit hook (`scripts/bump-version.js`) which ALWAYS does a minor bump** — every commit auto-bumps `package.json` (1.40.0 → 1.41.0 on the deployer's first attempt). Release engineers must use `git commit --no-verify` (or `git reset --soft HEAD~1` + restore + re-commit) to keep the intended version. **Recommendation: add a `SKIP_BUMP` env guard** to `bump-version.js` so `SKIP_BUMP=1 git commit` bypasses the bump without skipping ALL hooks (which also skips memory-schema validation).
- **npm-publish GitHub workflow triggers on push to `main`, NOT on tags** — `.github/workflows/npm-publish.yml` runs `npm publish` when a branch merges into `main` (via PR). Pushing a tag alone (`git push origin --tags`) does NOT trigger publish. **Deploy flow for this repo:** commit + tag + push branch, then open a PR to `main`; publish happens on merge.
- **SECURITY: `.env` was tracked in git with a real DATABASE_URL password** — the file contains a live 32-char PostgreSQL credential (`postgres@192.168.31.200`). It should have been gitignored from day one. **Remediation:** `git rm --cached .env`, add `.env` to `.gitignore`, rotate the credential (it is in git history regardless). Secrets-check the template: `.env.example` is safe, `.env` is not.
- **npx behavior for scoped packages with a bin** — `npx @scope/package` resolves the scoped package, detects the single `bin` entry (`ai-workflow-setup`), and runs it. Positional subcommands like `setup` are silently ignored by this CLI's parser (`parseCliArgs` maps only known flags; unrecognized args fall through harmlessly). The unscoped `npx ai-workflow-template` returns 404 because that name doesn't exist on the registry — scoped packages must be invoked with their full `@scope/name`.

**Agent tuning notes:**
- **implementer (bootstrap)**: Verify the package identity facts (name, scope, bin, publishability, npmrc auth) BEFORE the researcher investigates — ground truth prevents wasted research on wrong assumptions (e.g., whether the package was even published).
- **researcher**: When a consumer reports "X not found"/"not configured", check the code path for env loading before blaming the user's config — the consumer's `.env` was valid and correctly placed; the parent process just never read it. Also always validate the error/warning message itself (it contained a command that couldn't possibly work). Look for the SAME bug class in sibling entry points (CLI fixed earlier, MCP server missed) — search all scripts that read `process.env`.
- **implementer (planning)**: When two tasks edit the same file, merge them into one task to avoid parallel write races (the coder had to adapt T1+T5 → one task, T7+T8+T9 → one task). Keep the entry-point import (`bin/setup.js`) rather than per-module imports — it guarantees ALL phases benefit from env loading, and ESM hoisting makes the timing safe.
- **coder (parallel batches)**: Before executing a batch, detect same-file collisions and merge them — this pipeline's planned 5+5 parallel tasks were safely collapsed to 4+3. When adding a flag to `SUPPORTED_FLAGS`, remember to update THREE places: constants.js (flag map), index.js (handler), ui.js (help text) — plus tests. When restoring coverage, add tests that assert the new behavior (not just that code runs).
- **coder (MCP/env fixes)**: If one script in a family needed a dotenv fix, audit the whole family. The knowledgebase-cli.js fix (2026-07-30) should have prompted checking mcp-knowledgebase-server.js at the same time. For opencode MCP entries that need env vars, the `github` entry is the reference pattern (`"env": { "VAR": "$VAR" }`).
- **deployer**: Before committing a release, check for auto-bump hooks (`scripts/bump-version.js` via Husky pre-commit) and confirm the publish trigger in the workflow file (`npm-publish.yml` on push to `main`) — neither the version bump nor npm publish happens where you'd naively expect. Never commit a release without running `git diff` to confirm the version didn't change. Scan `git ls-files` for tracked secrets before release.
- **tracker**: When a pipeline produces spikes + a plan + a release, record the fix pattern (env loading via `dotenv/config` at entry points) as a UNIVERSAL rule, not a one-off — this is the third time this project hit the "script reads process.env without loading .env" bug (knowledgebase-cli 2026-07-30, setup pipeline 2026-08-01, MCP server 2026-08-02).

## Session: 2026-08-02 — Knowledgebase MCP Search Threshold Fix

**Pipeline:** Follow-up fix after the npm E404 + `.env` loading pipeline (research spike → coder → verify after restart → tracker)
**Coverage:** N/A (one-line production fix + verification; all 160 tests still passing)
**TDD Iterations:** 0 (ad-hoc follow-up fix, not a pipeline run)

**New knowledge:**
- **An empty search result does NOT mean the index is empty — check the similarity threshold against the embedding model's actual score range** — `scripts/mcp-knowledgebase-server.js` defaulted `threshold` to `0.6`, but the `all-MiniLM-L6-v2` (384-dim) embedding model produces cosine similarities in the range ~0.01–0.41 for this corpus. So ALL results were filtered out and `knowledgebase_knowledgebase_search` returned "No results found. The knowledgebase may be empty." even though 9 chunks were indexed and `knowledgebase_knowledgebase_stats` showed them. The CLI worked because it passes `undefined` → core engine default `0.0`. **Diagnostic rule:** when search returns empty but stats show chunks, run the same query through the CLI at threshold 0.0 to see the real similarity distribution, then compare against the tool's default threshold.
- **`||` vs `??` falsy-coercion bug** — `args.threshold || 0.6` coerces an explicit `threshold: 0` to `0.6` because `0` is falsy. Fixed with nullish coalescing: `args.threshold ?? 0.1`. **Universal pattern:** use `??` (nullish coalescing) for default values wherever `0`/`false`/`''` are legitimate explicit inputs — `||` silently overwrites them.
- **Code changes to an MCP server require a server/opencode restart to take effect** — after fixing the MCP server code, the running opencode session's MCP server process still had the old code in memory; the live `knowledgebase_knowledgebase_search` kept returning "No results" until opencode was restarted. **Universal pattern:** during development, verify a fix against a freshly spawned server process (spawn → `initialize` → `tools/list` → `tools/call`), then restart the host process to load the fix into the live tool. A working fresh handshake is NOT proof the live tool is fixed.
- **Verification workflow that worked** — the fix was verified two ways: (a) CLI search (`node scripts/knowledgebase-cli.js search "..."`) returned ranked results with real similarities (top 0.396), and (b) a full MCP handshake (spawn server → `initialize` → `tools/list` → `tools/call knowledgebase_search`) returned "### Result 1 (similarity: 0.396)" with no explicit threshold passed. After restarting opencode, the live `knowledgebase_search` returned 5 results (top 0.396) — confirming the fix end-to-end.
- **Threshold tuning is model-specific** — the `0.6` default was an arbitrary high value (likely copied from a different embedding model). For `all-MiniLM-L6-v2`, realistic thresholds are 0.1–0.2 for "somewhat related", 0.25–0.4 for "moderately related", 0.4+ for "strongly related". A threshold default must be chosen for the embedding model actually in use.

**Agent tuning notes:**
- **researcher**: CLI-search diagnostics are the fastest way to compare thresholds — running the SAME query through `knowledgebase-cli.js` at `--threshold 0.0` vs `--threshold 0.6` reproduced the bug exactly and quantified the real similarity range (0.01–0.41). When an MCP tool returns empty but the data exists, use the CLI to isolate whether the problem is the tool's defaults vs. the data itself.
- **coder**: When fixing a default-value bug (`||` → `??`), scan sibling call sites for the same falsy-coercion pattern — the same class of bug can hide in `args.limit || 5` (also in this handler) and any other `||` default.
- **tracker**: Record MCP-server restart requirements as a universal gotcha — live MCP tools run long-lived processes that do NOT hot-reload code changes; document the restart step in verification notes.
