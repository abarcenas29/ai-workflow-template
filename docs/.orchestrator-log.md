# Orchestrator Log

## Pipeline: Fix PG knowledgebase write bug for other projects (registerProject upstream)

- **Date:** Sun Aug 02 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (user approved "do it" for the proposed quick-fix pipeline; mandatory confirmation gates still enforced)
- **Trigger:** User reported bug: "other projects can't write to the pg database." Knowledgebase search surfaced documented root cause: `knowledgebase_index` MCP handler missing `registerProject()` call → FK violation (Postgres 23503) silently counted as "skipped".

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 0 | implementer | Bootstrap architecture context + memory bank check |
| 1 | implementer ⏸️ | Verify current state of `scripts/mcp-knowledgebase-server.js`; create fix plan |
| 2 | coder | Apply fix (registerProject before upsertChunks + any dotenv/env-block gaps) |
| 3 | unit-tester | Run tests, ensure no regressions |
| 4 | reviewer | Review the fix |
| 5 | tracker | Record docs to tracker-log |
| 6 | tracker | Persist lessons learned |

### Execution Log

**Step 0 — Bootstrap (implementer):** ✅ SUCCESS. Project already fully initialized: `docs/.architecture-context.md` present (85L), all 6 memory-bank core files present, `memory-bank/tasks/_index.md` present. No scaffolding created. Tech stack confirmed: Node.js ESM, Vitest, PostgreSQL + pgvector. Memory bank updated (activeContext, progress). Noted pre-existing gap: package.json `main` points to non-existent `index.js` (orphaned, not blocking).

**Step 1 — Implementer planning (implementer):** ✅ SUCCESS. Bug CONFIRMED PRESENT. Plan at `plan/fix-kb-registerproject-1.md`. Fix: (1) add `registerProject` to named import in `scripts/mcp-knowledgebase-server.js` line 28, (2) add `await registerProject(projectId, projectId)` between empty-chunks guard and `upsertChunks()` (~line 226-228). Already correct: dotenv, opencode.json env block, CLI path, registerProject export. 5 tasks / 3 phases: Batch A (T1, T2, T5) parallel, Batch B (T3) test file creation (8 scenarios — currently zero MCP server tests), Phase 3 (T4) full vitest validation. **Confirmation gate PASSED by user.**

**Step 2 — Coder (Batch A):** LAUNCHING T1+T2 (combined: both edit `scripts/mcp-knowledgebase-server.js` — same-file race would occur if run as two concurrent agents) + T5 (memory-bank update, independent) in parallel. Reasoning documented: T1/T2 share a file; combined to avoid read-modify-write clobbering.

**Step 2 — Coder (Batch A):** ✅ SUCCESS. T1: `registerProject` added to import (line 33). T2: `await registerProject(projectId, projectId);` inserted after empty-chunks guard, before `upsertChunks()` (lines 229-230). Semicolon used to match file style (minor cosmetic deviation). Verified: `node --check` OK, MCP stdio server starts, 49/49 cli+index tests pass, full suite 160/160 pass. T5: memory-bank activeContext/progress/tasks/_index updated, index synced (2213 vectors). Plan marked Phase 1 COMPLETE.

**Step 2 — Coder (Batch B):** LAUNCHING T3 (create `scripts/mcp-knowledgebase-server.test.js`, 8 scenarios).

**Step 2 — Coder (Batch B):** ✅ SUCCESS. Created `scripts/mcp-knowledgebase-server.test.js` — 11 tests covering TEST-01..08 + 3 extras (search degradation, list degradation, import integrity). Minimal testability seam added to MCP server: exported `handleToolCall` (body unchanged), `isDirectRun` guard for stdio bootstrap. 11/11 new tests pass, full suite 171/171 (160 baseline + 11). Plan Phase 2 COMPLETE.

**Step 3 — Unit-tester (T4, Phase 3):** LAUNCHING full Vitest suite validation + coverage review.

**Step 3 — Unit-tester (T4):** ✅ SUCCESS. Full suite 176/176 (10 files, 0 failures). Coverage: overall 40.93% (pre-existing, <90% gate flagged not blocker), mcp-knowledgebase-server.js improved 64.4%→79.66% stmts / 81.03% lines. Regression guard verified NON-VACUOUS via mutation test (removed registerProject line in temp copy → TEST-01 fails). Added 5 tests (11→16) covering empty-KB, stats, list fallbacks, unknown tool, upsert-reject path. Plan T4 + Phase 3 COMPLETE, plan status Completed.

**Step 4 — Reviewer:** LAUNCHING code review of the fix + tests.

**Step 4 — Reviewer:** ✅ SUCCESS. Verdict: **APPROVED WITH NITS**. No critical/major. Independent mutation test: 2 tests fail without the fix (non-vacuous confirmed). Correctness: placement + args `(projectId, projectId)` correct, graceful degradation preserved. Security: parameterized SQL, no injection/path issues. 6 minor nits (pre-existing mostly: search schema default 0.6 vs 0.1, raw err.message, whitespace projectId, TEST-05 redundant assert, plan doc wording, isDirectRun symlink theoretical). Recommended follow-ups: TEST-12 live-spawn smoke test, schema default alignment, optional hardening. No rework requested.

**Step 5 — Tracker (documentation):** LAUNCHING documentation recording.

**Step 5 — Tracker:** ✅ SUCCESS. Appended full pipeline record to `docs/tracker-log.md`, summary to `memory-bank/progress.md`, `docs/TRACKER-INDEX.md` updated, memory bank + PG knowledgebase re-indexed (2373 vectors). **Step 7 (persist lessons) COVERED by tracker in Step 5** — it appended a `## Session: 2026-08-02` entry (8 reusable knowledge items + agent tuning notes) to `.agents/instructions/learned-knowledge.instructions.md` and re-indexed it into the PG knowledgebase (1 new chunk, 10 updated). No duplicate delegation needed.

### FINAL STATUS: ✅ COMPLETE — all steps succeeded, no failures.

---

## Pipeline 2: Follow-up nits + TEST-12 smoke test

- **Date:** Sun Aug 02 2026
- **AutoConfirm:** true (user approved "yes" to follow-up work; mandatory confirmation gates still enforced)
- **Scope:** (1) search schema default 0.6→0.1, (2) redact raw err.message in error responses (redactConnectionString exists at knowledgebase-index.js:62), (3) whitespace-only projectId validation+trim, (4) TEST-05 redundant assert cleanup; plus TEST-12 live-spawn smoke test (graceful skip if DATABASE_URL unavailable). Skipping: theoretical isDirectRun symlink edge, plan-doc wording nit.

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | coder | Apply nit fixes |
| 2 | unit-tester | Full suite re-validation + attempt TEST-12 smoke test |
| 3 | reviewer | Re-review the changes |
| 4 | tracker | Record docs + lessons |

### Execution Log

**Step 1 — Coder (nits):** LAUNCHING nit fixes.

**Step 1 — Coder (nits):** ✅ SUCCESS. Nit 1: search schema default 0.6→0.1. Nit 2: local `redactConnectionString(message)` helper (deviation — engine fn not exported; local helper only rewrites URL tokens so generic errors stay readable, avoiding broken TEST-15/16); applied to outer catch. Nit 3: `!projectId?.trim()` validation + `const pid = projectId.trim()` flowed through chunkLearnedKnowledge/registerProject/messages. Nit 4: removed TEST-05 redundant toContain. Tests 16→18 (added whitespace-projectId reject + trim-flow). Verified: node --check OK, MCP file 18/18, full suite 178/178.

**Step 2 — Unit-tester:** LAUNCHING full suite re-validation + TEST-12 live-spawn smoke test attempt.

**Step 2 — Unit-tester:** ✅ SUCCESS. Full suite 178/178 confirmed (18/18 MCP file). TEST-12 live-spawn smoke test **PASSED** against live DB (DATABASE_URL present in .env, absent from process env): initialize handshake OK, `knowledgebase_index` with fresh projectId returned "Indexed 1 chunks, updated 0, skipped 0" → proves registerProject ran (no FK 23503). Cleanup verified (0 residue). SIGTERM path exercised. Mutation re-test: 3 tests now fail without the fix (TEST-01, TEST-16, TEST-06c) — guard strictly stronger. No issues found. Closes last coverage gap (direct-run bootstrap).

**Step 3 — Reviewer:** LAUNCHING re-review of nit fixes + smoke test evidence.

**Step 3 — Reviewer:** ⚠️ CHANGES REQUESTED (narrowly scoped). RegisterProject fix + tests APPROVED; 178/178 re-verified. Blocking item: local `redactConnectionString` helper FAILS OPEN on unix-socket-style authorities (`postgres://user:secret@/var/run/postgresql` → raw leak; `new URL()` throws → catch returns message unchanged). Required fix: regex credential-strip fallback in catch (e.g., `message.replace(/(postgres(?:ql)?:\/\/)([^@\s]+)@/i, '$1***@')`). Optional minors: redact ALL URL tokens (not just first), avoid stray `:` when port absent, trim projectId in knowledgebase_search (pre-existing, out of scope). TEST-12 evidence accepted as credible. Regression guard verified consistent.

**Step 2b — Coder (redaction hardening):** LAUNCHING required catch-fallback fix + related redaction minors.

**Step 2b — Coder (redaction hardening):** ✅ SUCCESS. Helper rewritten as regex-only fail-closed by construction: `message.replace(/(postgres(?:ql)?:\/\/)([^/\s]+)@/gi, '$1***@')` — no URL parsing to fail, redacts ALL tokens (global flag), no stray port colon, `[^/\s]+` (not reviewer's `[^@\s]+`) handles passwords containing `@`. JSDoc rationale added. Helper NOT exported; tests exercise outer-catch path. Tests 18→21 (TEST-17/18 unix-socket + ?host= cases, TEST-19 multi-token). 21/21 MCP file, full suite 181/181. node -e spot checks all pass. Plan §11 added.

**Step 3b — Reviewer (final sign-off):** LAUNCHING re-review of hardened redaction.

**Step 3b — Reviewer:** ✅ **APPROVED**. Prior 🟡 Major blocker RESOLVED. Verified fail-closed by construction (no URL parsing to fail), `[^/\s]+` strictly better than suggested `[^@\s]+` (handles passwords with `@`, no over-match on socket-only/host-only), 3 new tests non-vacuous (replayed OLD helper → all 3 would fail), full suite 181/181 independently re-run. Remaining minors (search projectId trim, `args.limit || 5` vs `??`, query-string params) — pre-existing/out of scope, recommend close with optional 1-line follow-up. Recommendation: APPROVE.

**Step 4 — Tracker (documentation):** LAUNCHING documentation recording for follow-up pipeline.

**Step 4 — Tracker:** ✅ SUCCESS. Appended Pipeline 2 entry to `docs/tracker-log.md`, summary to `memory-bank/progress.md`, learned-knowledge session (7 items + 4 agent tuning notes) appended + re-indexed into PG knowledgebase — returned "Indexed 1 chunks, updated 11, skipped 0" (incidental live confirmation of the fix). TRACKER-INDEX.md updated. Step 7 (persist lessons) COVERED by tracker.

### FINAL STATUS (Pipeline 2): ✅ COMPLETE — all steps succeeded. Final suite: 181/181. Reviewer APPROVED.

---

## Pipeline 3: Final minor-hygiene pass

- **Date:** Sun Aug 02 2026
- **AutoConfirm:** true (user approved "do a pass")
- **Scope:** (1) `knowledgebase_search` trim/validate `projectId` for parity with index handler, (2) `args.limit || 5` → `?? 5` so `limit: 0` is honored, (3) redact query-string params in URL tokens (belt-and-braces). Optional minors previously flagged by reviewer.

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | coder | Apply the 3 minor fixes |
| 2 | unit-tester | Re-validate full suite + verify new behavior |
| 3 | reviewer | Final sign-off |
| 4 | tracker | Record docs + lessons |

### Execution Log

**Step 1 — Coder (minors):** LAUNCHING minor fixes.

**Step 1 — Coder (minors):** ✅ SUCCESS. Fix 1: search `project_id: args.projectId?.trim() || undefined` (whitespace-only dropped to match-all — graceful degradation for optional filter, contrasts with index handler's required-write-target error). Fix 2: `args.limit ?? 5` honors `limit: 0`. Fix 3: redaction regex extended to redact query/fragment (`?***`) while keeping host/path + fail-closed-by-construction. Tests 21→25 (TEST-20 whitespace filter, TEST-20b trim flow, TEST-21 limit:0, TEST-22 query-string redaction; TEST-18 updated for ?host=). Full suite 185/185. Plan §12 added. Note: knowledgebase-cli.js still uses `topK || 5` + untrimmed projectId — out of scope by constraint.

**Step 2 — Unit-tester:** LAUNCHING independent validation.

**Step 2 — Unit-tester:** ✅ SUCCESS. Full suite 185/185 + MCP file 25/25 confirmed exactly. Non-vacuity via real temp mutations: `?? 5`→`|| 5` fails TEST-21; trim→no-trim fails TEST-20+20b; old redaction regex fails TEST-18+22; removing registerProject fails TEST-01/06c/16. Earlier fail-closed guarantees (TEST-17/19) still hold; TEST-15/16 readability intact. No issues. Workspace clean (temp dir removed). Plan §12.6 added.

**Step 3 — Reviewer:** LAUNCHING final sign-off of minor fixes.

**Step 3 — Reviewer:** ✅ **APPROVED**. 185/185 independently re-run; 12-case empirical regex probe all pass; git diff scope discipline confirmed (knowledgebase-index/cli untouched). Search-filter design decision (whitespace → match-all, deliberate asymmetry with index handler, documented in code comment) sound per graceful-degradation pattern. Fail-closed guarantees preserved (TEST-15/16/17/19 intact). All 4 tests non-vacuous (corroborated by unit-tester mutations). Only informational minors: search schema `limit` description "(1–50)" now that `?? 5` honors `0`; pre-existing engine negative-limit clamp issue; cosmetic header comment. No follow-ups required.

**Step 4 — Tracker (documentation):** LAUNCHING documentation recording for Pipeline 3.

**Step 4 — Tracker:** ✅ SUCCESS. Pipeline 3 entry appended to `docs/tracker-log.md`, progress.md summary added, learned-knowledge session (6 items + 4 agent tuning notes) appended + re-indexed into PG knowledgebase ("Indexed 1 chunks, updated 12, skipped 0" — incidental live confirmation of fix). TRACKER-INDEX.md updated. Step 7 (persist lessons) COVERED by tracker.

### FINAL STATUS (Pipeline 3): ✅ COMPLETE — all steps succeeded. Final suite: 185/185. Reviewer APPROVED. All nits/minors closed.

---

## Pipeline 4: README.md trim + instructions-on-top

- **Date:** Sun Aug 02 2026
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (direct user request)
- **Trigger:** "modify the README.md cut down the fluff. and put the instructions on the very top of the page."

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | coder | Trim fluff from README.md; move install/setup instructions to the very top |
| 2 | reviewer | Verify content accuracy, no broken links/sections |
| 3 | tracker | Record docs + lessons |

### Execution Log

**Step 1 — Coder (README edit):** LAUNCHING README trim + restructure.

**Step 1 — Coder (README edit):** ✅ SUCCESS. README.md rewritten 469→289 lines (−38%). New order: title → one-line desc → **Install** → **Git Hook Setup** (first screen) → condensed capabilities → Sync → Core Structure → Orchestrator → TDD → Memory Bank → Knowledge Graph → Featured Skills → Versioning → Publish → Local Dev → Updating → Notes. Fluff removed: emoji decoration, salesy intro, verbose run-flow list, YAML example → 1 sentence. Accuracy fixes: skills count 27→29 (verified), removed non-existent `.agents/compress/`+`.agents/agents/`, corrected orchestrator path to `.opencode/agents/orchestrator/orchestrator.agent.md`, added `--knowledgebase` setup option. All commands verified against package.json scripts + `--help`. markdownlint 0 structural errors (only default MD013 line-length notices). Instructions confirmed at top (lines 1-51).

**Step 2 — Reviewer:** LAUNCHING review of rewritten README.

**Step 2 — Reviewer:** ✅ **APPROVED**. Instructions at top confirmed (Install line 5, Git Hook Setup line 13). Fluff cut without losing actionable content (diff audit). All 4 accuracy spot-checks independently verified (skills 29, orchestrator path exists, --knowledgebase real, removed dirs don't exist). Markdown quality improved (0 structural errors, was failing several). Minor informational nit: Featured Skills lists `graphify` as package skill but it's global (`~/.config/opencode/skills/`) — only `graphify-framework-aware` shipped. No rework required.

**Step 1b — Coder (README nit):** LAUNCHING one-line accuracy fix for graphify skill listing.

**Step 1b — Coder (README nit):** ✅ SUCCESS. README.md line 225: `graphify` qualified as `(global skill)` + added the actually-shipped `graphify-framework-aware` sibling. Verified global skill exists, package skill does not. markdownlint 0 structural errors (only pre-existing MD013 line-length). README still 289 lines.

**Step 3 — Tracker (documentation):** LAUNCHING documentation recording for Pipeline 4.

**Step 3 — Tracker:** ✅ SUCCESS. Pipeline 4 entry appended to `docs/tracker-log.md`, progress.md updated, learned-knowledge session (6 items + 4 tuning notes) appended + re-indexed into PG knowledgebase ("Indexed 1 chunks, updated 13, skipped 0"). TRACKER-INDEX.md updated. Step 7 (persist lessons) COVERED by tracker.

### FINAL STATUS (Pipeline 4): ✅ COMPLETE — all steps succeeded. README 469→289 lines, instructions at top, reviewer APPROVED.

---

## Pipeline 5: Auto-update vocabulary JSON (vocab-sync) + agent instructions

- **Date:** Mon Aug 03 2026
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (user said "Do your recommendation. Yes spin a pipeline." — mandatory confirmation gates still enforced)
- **Trigger:** Pre-commit warnings `[float32array, learned-knowledge, npm, package-structure, readme]` from `validate-memory-schema.js`; user asked how to auto-update `memory-bank/.vocabulary.json`.
- **Scope (user-approved plan):**
  - L1A: "Maintaining the Vocabulary" section in `.agents/instructions/memory-schema.instructions.md`
  - L1B: Memory-bank hygiene checklist in `.opencode/agents/reviewer.agent.md`
  - L2: New `scripts/vocab-sync.js` (+ tests) auto-appending unknown tags to `.vocabulary.json`, **wired into `.husky/pre-commit`**
  - Validator fix: count `entity_patterns` keys as known tags (so `npm`-style warnings stop)
  - **Distribution:** changes must propagate to consumer projects (setup tool hook template + sync mechanism)

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 0 | implementer | Bootstrap architecture context + memory bank check |
| 1 | implementer ⏸️ | Investigate distribution mechanism (setup/ + sync.js); create implementation plan |
| 2 | coder | Implement vocab-sync.js, validator fix, instructions, reviewer, hook wiring |
| 3 | unit-tester | vocab-sync + validator tests, full suite validation |
| 4 | reviewer | Quality/security review |
| 5 | tracker | Record docs + persist lessons |

### Execution Log

**Step 0 — Bootstrap (implementer):** ✅ SUCCESS. `docs/.architecture-context.md` present (85L, real content), all memory-bank core files present. Regenerated missing `graphify-out/graph.json` (2879 nodes, 3140 edges, 265 communities). Memory vector index synced (2963 vectors). Tech stack confirmed: Node.js ESM, Vitest (90% coverage gate), Husky v9, OpenCode agents, sqlite-vec + PG vector. activeContext/progress updated. No scaffolding created.

**Step 1 — Implementer planning (implementer):** ✅ SUCCESS. Plan at `plan/feature-vocab-sync-1.md` — 10 tasks, Batch A (7 parallel) + Batch B (3 parallel). Distribution mechanism mapped: scripts ship via npm tarball + `sync.js` `scriptsToSync` (vocab-sync.js must be added), `.husky/pre-commit` written by setup tool `constants.js` TEMPLATE_HOOKS, `.opencode/**` and `.agents/**` synced by sync.js. Key decisions: staged-files only, `git add` vocab in-hook, default `topic` group, no shared module extraction, vocab-sync runs BEFORE validator (kills chicken-and-egg). Risks: old consumers need template update + re-setup. ⏸️ **Confirmation gate PENDING user.**

**Confirmation gate PASSED by user.**

**Step 2 — Coder (Batch A):** LAUNCHING 7 parallel coder tasks — T1 (vocab-sync.js), T3 (validator entity_patterns), T4 (setup constants hook template), T5 (.husky/pre-commit), T6 (sync.js scriptsToSync), T8 (memory-schema instructions), T9 (reviewer agent).

**Step 2 — Coder (Batch A):** ✅ SUCCESS 7/7. T1: `scripts/vocab-sync.js` created (212L; staged-scan, entity_patterns-aware known set, kebab-case normalization, topic-group append, git add try/catch, 8 exports + isDirectRun seam; node --check + temp-git e2e + full suite 185/185 pass). T3: validator counts `entity_patterns` keys as known tags (npm/vitest no longer warn; random tag still warns; `|| {}` guard; 185/185). T4: setup `constants.js` TEMPLATE_HOOKS pre-commit adds `node scripts/vocab-sync.js` between bump-version and validate-memory-schema + description updated (setup suite 104/104; expectedContent() auto-derives so no test edits). T5: `.husky/pre-commit` line added, executable bit preserved (100755). T6: `sync.js` scriptsToSync + `'vocab-sync.js'` adjacent to validator (node -e introspection confirmed). T8: `memory-schema.instructions.md` +22L "Maintaining the Vocabulary" section. T9: `reviewer.agent.md` memory-bank hygiene bullet added. Batch A files all disjoint, no clobbering observed.

**Step 2 — Coder (Batch B):** LAUNCHING 3 parallel coder tasks — T2 (vocab-sync.test.js), T7 (setup hooks test verify), T10 (validate-memory-schema.test.js + export seam).

**Step 2 — Coder (Batch B):** ✅ SUCCESS 3/3. T2: `scripts/vocab-sync.test.js` created — 54 tests covering TEST-01..14 via real temp-git-repo fixtures (chdir + resetModules + dynamic import), spawnSync CLI exit-code suite, isDirectRun branch covered in-process; vocab-sync.js coverage 98.88% stmts / 92.15% branches / 100% funcs / 100% lines; full suite 254/254 across 12 files. T7: setup suite verify — NO test changes needed (expectedContent() auto-derives from template; marker-first idempotency invariant intact); setup suite 104/104. T10: `scripts/validate-memory-schema.test.js` created — 15 tests (TEST-01..05 entity_patterns scenarios + main() exit-code contract + validateFile error branches); added isDirectRun guard + export seam to validator (direct-run behavior identical, CLI exit 0 verified); 15/15 pass. Plan status marked Completed. Transient mid-flight T2 failure noted by T10 was resolved in T2's final state.

**Step 3 — Unit-tester (independent validation):** LAUNCHING full-suite re-run + coverage gate + post-B smoke checks.

**Step 3 — Unit-tester:** ✅ SUCCESS — PASS, NO ISSUES. Full suite 254/254 (12 files, 0 failures). Coverage: vocab-sync.js 98.88% stmts / 92.15% branch / 100% funcs / 100% lines; validator 92.39% / 92.85% / 100% / 92.13% (both ≥ 90% gate). Mutations prove non-vacuity: removing entity_patterns merge → TEST-01/04 fail; disabling append → 14 tests fail. TEST-05 pre-commit smoke (scratch temp repo): vocab-sync appended `brand-new-tag`, validator then ran clean with no unknown-tag warning (chicken-and-egg eliminated), 2nd run no-op (idempotent). TEST-06 setup suite 104/104, marker-first invariant intact. TEST-07 scriptsToSync + npm files include vocab-sync.js. No issues.

**Step 4 — Reviewer:** LAUNCHING code review of vocab-sync feature.

**Step 4 — Reviewer:** ⚠️ CHANGES REQUESTED (narrowly scoped). No critical findings. 254/254 + setup 104/104 independently re-verified; coverage ≥ 90% both files; ordering, marker-idempotency, scriptsToSync mechanism, npm files, command-injection (static args only) all confirmed. **M1 (major):** validator compares RAW tags while vocab-sync normalizes → non-normalized tags (`My Tag`) still warn after vocab-sync appends `my-tag`; chicken-and-egg only resolved for already-normalized tags; no test covers this (all 15 validator tests use lowercase). Fix: normalize both sides in `validateFile()` + add test. **M2 (major):** T8/T9 instruct "add a one-line comment in JSON" — impossible in strict JSON; following literally corrupts vocab. Fix: reword to rationale in commit message/PR or schema description strings. Minors: (1) `buildKnownTags` char-iterates if `vocab.tags` is an array (consumer-controlled, add Array.isArray guard); (2) first write canonicalizes blank-line spacing (informational); (3) `sync.test.js` fixture omits vocab-sync.js (wouldn't catch scriptsToSync regression); (4) memory files introduce unknown tags — self-healing by design (informational).

**Step 2b — Coder (review fixes):** LAUNCHING M1 + M2 + minors 1/3 fixes.

**Step 2b — Coder (review fixes):** ✅ SUCCESS. M1: validator normalizes BOTH sides before unknown-tag filter (same normalizeTag: lowercase/trim/underscore+space→hyphen/drop-empty) — `My Tag` no longer warns once `my-tag` in vocab; +3 validator tests (M1 describe block). M2: reworded T8 step 5 + T9 bullet — rationale in commit message/PR or `categories` description strings, explicit "strict JSON, no comment syntax" warning. Minor 1: `Array.isArray(group)` guard in buildKnownTags + 2 malformed-shape tests. Minor 3: `vocab-sync.js` added to sync.test.js fixture (all 7 comments). `.vocabulary.json` untouched. Verified: targeted 81/81, full suite 259/259 (12 files, 0 failures), coverage validator 92.7/93.1 stmts/branch, vocab-sync 98.91/92.15.

**Step 3b — Reviewer (final sign-off):** LAUNCHING re-review of M1/M2/minor fixes.

**Step 3b — Reviewer:** ✅ **APPROVED**. No critical/major. M1 verified (normalization parity in validator lines 131-146, +3 tests, empirical scratch-repo chicken-and-egg re-check: `My Tag` → vocab-sync appended `my-tag` → validator NO warning exit 0; controls still warn). M2 verified (both docs reworded, grep confirms "one-line comment in the JSON" removed). Minor 1 guard before iteration; Minor 3 fixture matches real array. Independent re-runs: full suite 259/259, setup 104/104, coverage validator 92.7/93.1, vocab-sync 98.91/92.15. Previously-approved aspects intact (ordering indices 4<5, marker-first idempotency, warn-not-block, entity_patterns, idempotency). Only informational minors: stale plan wording (line 221), normalizeTag duplication drift risk (CON-01 inherent), pre-existing unknown tags self-heal by design. Recommendation: APPROVE, no further changes.

**Step 5 — Tracker (documentation):** LAUNCHING documentation recording + lessons learned.

**Step 5 — Tracker:** ✅ SUCCESS. Full Pipeline 5 entry appended to `docs/tracker-log.md` (9-step execution table, 10 files table, 8 key decisions, verification results). `memory-bank/progress.md` summary added (index 630 vectors). Learned-knowledge Session 2026-08-03 appended (9 knowledge items + 5 agent tuning notes) and re-indexed into PG knowledgebase ("Indexed 1 chunks, updated 14, skipped 0"). TRACKER-INDEX.md updated. **Step 7 (persist lessons) COVERED by tracker.**

### FINAL STATUS (Pipeline 5): ✅ COMPLETE — all steps succeeded. Final suite 259/259 + setup 104/104, coverage ≥ 90%, reviewer APPROVED. Feature: vocab-sync auto-updates `.vocabulary.json` in pre-commit (runs before validator), wired to consumers via setup hook template + sync.js; instructions + reviewer updated.

---

## Pipeline 6: Audit — do agents tap the central knowledge vector?

- **Date:** Mon Aug 03 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** false (research question; step-by-step confirmation)
- **Trigger:** User asked: "Does my agents tap on my central knowledge vector in its implementation, design, architect, review and other information based agents that would help them provide better decision making and prevention of repeated mistakes?"

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | researcher ⏸️ | Audit agent specs (.opencode/agents/*.agent.md, .agents/agents/*.agent.md, orchestrator prompt templates) + knowledge infrastructure (knowledgebase MCP, memory-bank, learned-knowledge.instructions.md, memory-search skill) to determine which agents reference the central knowledge vector and how |

### Execution Log

**Step 1 — Researcher (knowledge-vector audit):** ✅ SUCCESS. Full audit returned in-message (no files written). Key findings: (1) **Layer 3 PG knowledgebase completely unreferenced** — `knowledgebase.instructions.md` declares "MANDATORY REQUIREMENT" but zero agent specs or orchestrator templates inject `knowledgebase_search`; 17 sessions of learned knowledge are write-only for the vector. (2) **learned-knowledge.instructions.md write-only for most agents** — main orchestrator never injects its reading; only feature-pipeline + TDD Phase 1 do. (3) **5 agents (deployer, e2e-tester, researcher, reviewer, unit-tester) have zero knowledge retrieval** despite having memory-bank/* and knowledgebase/* permissions. (4) **Orchestrator injection asymmetry** — feature-pipeline injects architecture+learned+memory, main orchestrator only memory-bank, TDD reviewer gets zero. (5) Tracker writes but never reads learned-knowledge before writing. Recommendations: P0 add knowledgebase_search + learned-knowledge reading to specs/orchestrators; P1 close gaps for the 5 knowledge-less agents + parallel coders + TDD reviewer. ⏸️ Confirmation gate PASSED by user (chose "1. Close the gap." → Pipeline 7).

---

## Pipeline 7: Close knowledge-vector gaps with graceful-failure KB access

- **Date:** Mon Aug 03 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (user chose "1. Close the gap." — mandatory confirmation gates still enforced)
- **Trigger:** Pipeline 6 audit findings (researcher). Gaps: PG knowledgebase (Layer 3) completely unreferenced by agents/orchestrators; learned-knowledge.instructions.md write-only for most agents; 5 agents (deployer, e2e-tester, researcher, reviewer, unit-tester) have zero knowledge retrieval; orchestrator injection asymmetry (feature-pipeline injects architecture+learned+memory, main orchestrator only memory-bank, TDD reviewer zero).
- **User requirement:** Close the gaps BUT with graceful failure — if the PG vector is not set up, agents must NOT fail: they must explicitly notify that knowledgebase search was NOT executed; and when the vector IS accessed, explicitly notify that it was accessed. Be explicit in toolcalls for this.

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | implementer ⏸️ | Create implementation plan: wire knowledgebase_search + learned-knowledge reading into all agent specs + orchestrator templates with graceful-failure/explicit-notification protocol |
| 2 | coder | Apply changes (likely parallel batches across agent spec files + orchestrator templates + instructions) |
| 3 | unit-tester | Validate: spec/template integrity, script changes, graceful-failure behavior |
| 4 | reviewer | Review wiring + graceful-degradation correctness |
| 5 | tracker | Record docs + persist lessons |

### Execution Log

**Step 1 — Implementer (planning):** ✅ SUCCESS. Plan at `plan/feature-knowledge-vector-gaps-1.md`. 18 tasks / 4 batches: Batch A (T1-T2: shared protocol file + knowledgebase.instructions.md reconciliation), Batch B (T3-T12: 10 agent specs reference the shared protocol — incl. adding retrieval step to the 5 previously knowledge-less agents), Batch C (T13-T15: 3 orchestrator templates made symmetric — all delegation types get memory-bank + learned-knowledge + knowledgebase with graceful failure), Batch D (T16-T18: 2 grep-based unit tests + manual graceful-failure verification). Key decisions: shared protocol file `.agents/instructions/knowledge-retrieval.instructions.md` (single source of truth, prevents duplication drift) over inline duplication; graceful-failure as governing constraint (mandatory to ATTEMPT, graceful to fail, explicit-report in final summary); plan.agent.md out of scope (standalone pre-pipeline agent); tracker gets pre-append read requirement. 1 new file + 2 new test files + 14 modified (docs/spec wiring only, 0 production code). ⏸️ **Confirmation gate PASSED by user.**

**Step 2 — Coder (Batch A):** LAUNCHING T1+T2 in parallel (disjoint files: new protocol file vs knowledgebase.instructions.md).

**Step 2 — Coder (Batch A):** ✅ SUCCESS 2/2. T1: `.agents/instructions/knowledge-retrieval.instructions.md` created (57L; 3-layer retrieval order memory-bank → learned-knowledge → knowledgebase → project files; graceful-failure wrapper try/catch; explicit-report disclosure mandatory in final summary; exact report phrases "knowledgebase_search executed — N results returned." / "knowledgebase_search NOT executed (PG vector unavailable)."). T2: `knowledgebase.instructions.md` reconciled (+36/−15): "MANDATORY REQUIREMENT — NOT BEST EFFORT" → "MANDATORY TO ATTEMPT, GRACEFUL TO FAIL"; explicit-report protocol added; 3 cross-refs to protocol file; operational guidance intact; verified no "must succeed/required to function" language. Both verified via grep. T1 noted lowercase-vs-titlecase "explicitly note" test-match concern for T16 (plan text uses "Explicitly note") — flagged for Batch D.

**Step 2 — Coder (Batch B):** LAUNCHING 10 parallel coder tasks — T3..T12 (agent specs: implementer, architect, deployer, designer, e2e-tester, coder, researcher, reviewer, tracker, unit-tester; T5/T7/T9/T10/T12 are the 5 previously knowledge-less agents getting a NEW "Check Knowledge" step).

**Step 2 — Coder (Batch B):** ✅ SUCCESS 10/10. Plan's authoritative FILE mapping followed (coders verified against plan; mapping differed from orchestrator's initial guess: T3→architect, T4→coder, T8→implementer). All 10 agent specs now reference `.agents/instructions/knowledge-retrieval.instructions.md` + call `knowledgebase_knowledgebase_search` wrapped in graceful failure + "State the result in your final summary". Details: architect (T3, +1L step 3 extension), coder (T4, +1L), deployer (T5, NEW step 3 "Check Knowledge", renumbered), designer (T6, NEW step 4 "Query Knowledgebase", renumbered 1-7), e2e-tester (T7, NEW step 3 "Check Knowledge"), implementer (T8, +1L), researcher (T9, NEW `### 0a. Check Knowledge` before Investigation Planning, renumbered 0a/0b/1-5), reviewer (T10, NEW step 0 "Check Knowledge"), tracker (T11, NEW step 1 "Check Knowledge" incl. pre-append learned-knowledge read requirement + renumbered 1-6), unit-tester (T12, NEW step 1 "Check Knowledge"). All verified: YAML frontmatter intact, sequential numbering, grep checks pass, scope discipline (1 file each), memory index synced. Coders executed the protocol themselves — KB executed with 0-5 results (empty vector, no failures).

**Step 2 — Coder (Batch C):** LAUNCHING 3 parallel coder tasks — T13 (orchestrator.agent.md), T14 (feature-pipeline.agent.md), T15 (tdd-orchestrator.agent.md) — make all delegation templates symmetric.

**Step 2 — Coder (Batch C):** ✅ SUCCESS 3/3. T13 (main orchestrator, +3 insertions): standard delegation gets `- Knowledge retrieval:` line + `- Architecture context:` line (previously missing); parallel-coder delegation gets knowledge-retrieval line. T14 (feature-pipeline, +4): standard delegation gains `- Knowledgebase:` line; parallel-coder delegation gains the previously-dropped `- Learned knowledge:` + `- Architecture context:` + `- Knowledgebase:` lines. T15 (TDD, +6/0): all 6 phase delegation templates get identical `- Knowledge retrieval:` line — Phase 5 reviewer zero-injection gap FIXED. All verified: exactly N injection lines per template (no dupes), code fences balanced, YAML intact, markdownlint no new structural errors, scope discipline. Coders executed protocol: KB executed 0-5 results (empty vector).

**Step 2 — Coder (Batch D):** LAUNCHING 3 parallel coder tasks — T16 (tests/spec-knowledge-retrieval.test.js), T17 (tests/spec-orchestrator-parity.test.js), T18 (manual graceful-failure self-consistency verification).

**Step 2 — Coder (Batch D):** ✅ SUCCESS 3/3. T16: `tests/spec-knowledge-retrieval.test.js` created (6 tests: 10 agent specs ref protocol, learned-knowledge refs, 3 orchestrators ref protocol, shared file has graceful + explicit-report case-insensitive, NOT BEST EFFORT removed, plan.agent correctly excluded). Deviation: `vitest.config.ts` include extended to `tests/**/*.test.{js,ts}` (plan's validation command would otherwise find no tests; doesn't affect Playwright .spec.ts). 6/6 pass, full suite 13 files / 265 / 0 failures. T17: `tests/spec-orchestrator-parity.test.js` created (8 tests: robust fenced-block extraction + marker classification; main orchestrator standard + parallel-coder refs; feature-pipeline both delegations have learned-knowledge+architecture-context+knowledgebase; TDD all 6 phases incl. Phase 5 reviewer; occurrence-count guards exact 2×/6×/2×). 8/8 pass, full suite 14 files / 273 / 0 failures. T18: manual graceful-failure self-consistency verification PASS — no violations; 0 hits for hard-dependency language (only the correct governing negation at knowledge-retrieval.instructions.md:8); explicit-report requirement in shared file (:48-49) + ALL 3 orchestrators (exceeds "at least one" bar); tracker pre-append read confirmed (tracker.agent.md:25,29); knowledgebase.instructions.md reconciled (0 hits MANDATORY REQUIREMENT/NOT BEST EFFORT). Plan marked fully Completed (18/18 tasks, 4 batches).

**Step 2 — Coder (combined):** ✅ SUCCESS 18/18 tasks across 4 batches (2+10+3+3). Files: 1 new protocol file, 2 new test files, 1 vitest.config.ts line, 10 agent specs, 3 orchestrator templates, knowledgebase.instructions.md. Plan status Completed.

**Step 3 — Unit-tester (independent validation):** LAUNCHING full-suite re-run + spec-wiring verification.

**Step 3 — Unit-tester:** ✅ SUCCESS — PASS, NO ISSUES. Full suite 14 files / 273 / 0 failures independently re-run (matches coder claim exactly; `fatal: not a git repository` lines are expected vocab-sync temp-git fixtures). New spec-wiring tests 14/14. Independent spot-checks (NOT trusting coder summaries): reviewer:28, unit-tester:28, deployer:31 have "Check Knowledge"; all 10 agent specs reference protocol; main orchestrator injections at :124/:158 INSIDE fenced blocks + arch-context :125; feature-pipeline :106/:143 both carry full set; TDD :135/162/206/249/290/315 all 6 phases; shared protocol phrases at :8/:25/:29/:30 + explicit-report :48-49; knowledgebase.instructions.md reconciled (0 hits NOT BEST EFFORT); tracker pre-append read :25/:29; all 13 modified agent files YAML-parse. Coverage: global 48.12% (pre-existing, no production code changed this pipeline); recently-changed scripts all ≥90%. No issues.

**Step 4 — Reviewer:** LAUNCHING code review of the knowledge-vector gap closure.

**Step 4 — Reviewer:** ✅ **APPROVED** (recommend addressing 🟡 M1+M2 before merge). No critical. Independent verification: full suite 14 files/273/0, targeted 14/14, YAML 13/13, fences 13/13 even, markdownlint non-MD013 identical to HEAD, hard-dependency grep 0 hits, exact-string occurrence counts. Graceful-failure requirement FULLY MET: no hard-dependency language (only correct negation at knowledge-retrieval.instructions.md:8), explicit-report non-optional (:48-49) + all 6 delegation templates, exact phrases at :29-30, knowledgebase.instructions.md reconciled. 🟡 M1: tracker.agent.md:34 stale "determined in step 3" (renumbered → should be step 4). 🟡 M2: layer-numbering drift — protocol L1=Memory/L2=Learned/L3=KB vs knowledgebase.instructions.md:222-224 storage taxonomy + :207 header. 🔵 B1: phrase drift "N results returned." vs "N results." at knowledgebase.instructions.md:178. 🔵 B2: e2e-tester step order deviates from plan T7 (Check Knowledge after Open Page). 🔵 B3: feature-pipeline.agent.md:140-143 fence bullets at column 0 (cosmetic). 🔵 B4: main orchestrator parallel-coder lacks arch-context (feature-pipeline has it). Tests sound + non-vacuous (TEST-09/10 catch asymmetry regressions; occurrence-count guards catch duplicates). Scope discipline clean, 0 production code. Reviewer also actioned memory-bank hygiene: added knowledge-vector-gaps/graceful-failure/agents to .vocabulary.json.

**Step 4b — Coder (review fixes):** LAUNCHING fixes for M1+M2 + minors B1-B4 (doc-only one-liners).

**Step 4b — Coder (review fixes):** ✅ SUCCESS. M1: tracker.agent.md:34 "step 3" → "step 4". M2: knowledgebase.instructions.md storage taxonomy relabeled "Layer N" → "Tier N" (Tier 1 Markdown/Tier 2 Memory Bank/Tier 3 Knowledgebase) + explicit disambiguation (storage tiers ≠ retrieval layers) + table header fixed — eliminates L1 collision. B1: :178 "N results." → "N results returned." (canonical parity). B2: e2e-tester.agent.md Check Knowledge now step 2, Open Page step 3 (matches plan T7 intent), renumbered. B3: feature-pipeline.agent.md:140-143 parallel-coder fence bullets re-indented to 6 spaces. B4: orchestrator.agent.md:159 parallel-coder gets architecture-context line (full cross-orchestrator parity with feature-pipeline). Shared protocol NOT touched, graceful-failure wording unchanged. Verification: targeted 14/14, full suite 14 files/273/0, occurrence-count invariants unchanged, fences balanced. No test updates needed.

**Step 3b — Reviewer (final sign-off):** LAUNCHING re-review of fix pass.

**Step 3b — Reviewer:** ✅ **APPROVED** — no findings (0 critical / 0 major / 0 minor). All 6 fixes independently verified (M1: tracker step 4 cross-ref + sequential 1-6; M2: storage "Tier N" taxonomy + disambiguation, grep "Layer [0-9]" = 0 hits; B1: canonical "N results returned." parity; B2: e2e-tester Check Knowledge step 2 before Open Page; B3: fence bullets at 6 spaces consistent; B4: parallel-coder arch-context, occurrence-count guards not tripped). Graceful-failure contract intact: shared protocol 0 diff in fix round, hard-dependency scan 0 hits, "MANDATORY TO ATTEMPT, GRACEFUL TO FAIL" retained. Targeted 14/14 + full suite 14 files/273/0 independently re-run. Memory-bank hygiene clean (validator exit 0). Recommendation: APPROVE — ready to merge.

**Step 5 — Tracker (documentation):** LAUNCHING documentation recording + lessons learned.

**Step 5 — Tracker:** ✅ SUCCESS. Full Pipeline 7 entry appended to `docs/tracker-log.md` (execution table, 19-file table, 8 key decisions, verification results). `memory-bank/progress.md` summary added. `docs/TRACKER-INDEX.md` updated (Pipeline 7 + Pipeline 6 audit rows). Learned-knowledge Session 2026-08-03 appended to `.agents/instructions/learned-knowledge.instructions.md` (9 reusable knowledge items + 6 agent tuning notes: shared-protocol-file pattern, graceful-failure contract, grep-based spec tests w/ occurrence-count guards, orchestrator symmetry check, plan-authoritative FILE mapping, layers-vs-tiers, phrase parity, vitest include gotcha, tracker pre-append read). **Step 7 (persist lessons) COVERED by tracker.** Tracker exercised the protocol end-to-end: knowledgebase_search executed (0 results, vector empty at that moment), pre-append read verified against 13 prior sessions, then knowledgebase_index re-indexed ("Indexed 16 chunks, updated 0, skipped 0" — incidental live confirmation of the registerProject fix). Memory bank synced (1561 vectors).

### FINAL STATUS (Pipeline 7): ✅ COMPLETE — all steps succeeded. Full suite 14 files / 273 / 0 + setup unchanged; reviewer APPROVED (after M1+M2+B1-B4 fix pass, no findings). Feature: ALL agents + orchestrators now tap the central knowledge vector (memory-bank + learned-knowledge + knowledgebase) with the user's graceful-failure contract — mandatory to attempt, graceful to fail, explicit-report disclosure non-optional. 1 new protocol file, 10 agent specs wired, 3 orchestrators symmetric, 2 spec-wiring test files, knowledgebase.instructions.md reconciled.

---

## Pipeline 8: Investigate — why is the knowledgebase "empty" when indexing reported success?

- **Date:** Mon Aug 03 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** false (research question; step-by-step confirmation)
- **Trigger:** User asked: "why is it empty was it filled before?" — Pipeline 7 agents consistently reported "knowledgebase_search executed — 0 results returned (vector empty)", yet the tracker's final `knowledgebase_index` reported "Indexed 16 chunks, updated 0, skipped 0" and earlier pipelines (2-5) reported incremental indexing ("Indexed 1 chunks, updated 12-14"). Contradiction suggests a search-side issue (projectId filter / threshold / misdiagnosis), not a truly empty store.

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | researcher ⏸️ | Diagnose: was the KB filled before? Why do searches return 0 while indexing reports chunks? (stats/list tools, MCP search implementation, projectId semantics, threshold, historical indexing trail) |

### Execution Log

**Step 1 — Researcher (KB emptiness diagnosis):** ✅ SUCCESS. Verdict: **NOT empty — query-misconfigured + stale MCP server**. Store state: 5 projects / 52 chunks / 8.5MB (`@abarcenas/ai-workflow-template` 15 chunks, `ai-workflow-template` 16 chunks, `@abarcenas/megane-ui-lib` 12, `cms-apmc` 3, `megane-excercise` 6). Fill history: pipelines 2-5 indexed incrementally under the SCOPED projectId `@abarcenas/ai-workflow-template` (12→13→14→15); Pipeline 7 tracker indexed 16 NEW chunks under the UNscoped `ai-workflow-template` (projectId split — package.json name is `@abarcenas/ai-workflow-template`). ROOT CAUSE of 0-results: similarity threshold mismatch — old default 0.6 filtered everything because the `Xenova/all-MiniLM-L6-v2` (384-dim) model produces cosine similarities ~0.01-0.48; the fix to `?? 0.1` was ALREADY applied (mcp-knowledgebase-server.js:192) but Pipeline 7 sub-agents' MCP processes likely loaded a stale copy (no restart after fix — a documented learned-knowledge gotcha). Repro matrix: threshold 0.1 → 5 results; 0.45 → 3; 0.6 → 0. Secondary data-quality issue: duplicate project split (`@abarcenas/ai-workflow-template` vs `ai-workflow-template`, identical content). Recommendations: (1) restart opencode/MCP server to pick up threshold fix; (2) optionally consolidate duplicate projects + standardize projectId (always scoped package.json name). ⏸️ Confirmation gate PASSED by user (chose option 1 → Pipeline 9).

---

## Pipeline 9: Consolidate duplicate KB projects + standardize projectId convention

- **Date:** Mon Aug 03 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (user chose "do option 1" — mandatory confirmation gates still enforced)
- **Trigger:** Pipeline 8 diagnosis (researcher). Store has duplicate project split: `@abarcenas/ai-workflow-template` (15 chunks, pipelines 2-5) vs `ai-workflow-template` (16 chunks, pipeline 7). package.json name is the scoped `@abarcenas/ai-workflow-template`. Also confirmed: threshold fix (`?? 0.1`) already in code but requires MCP server restart to take effect operationally.
- **Scope:** (1) Consolidate duplicate projects in the PG knowledgebase under the canonical scoped projectId (delete `ai-workflow-template` rows, ensure `@abarcenas/ai-workflow-template` holds all chunks, no data loss — content identical); (2) Standardize projectId derivation so the tracker/orchestrator/CLI always use the scoped package.json `name` when calling `knowledgebase_index` (prevent recurrence); (3) Note/verify the restart requirement is documented.

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | implementer ⏸️ | Plan: data-consolidation steps (exact SQL/ops), projectId standardization points (tracker.agent.md? orchestrator templates? knowledgebase-cli.js default? mcp server? protocol file?), verification strategy |
| 2 | coder | Apply changes: consolidate DB rows, wire standardization, update docs |
| 3 | unit-tester | Validate: store state (stats/list), tests still pass, no data loss |
| 4 | reviewer | Review data operation safety + convention change |
| 5 | tracker | Record docs + persist lessons |

### Execution Log

**Step 1 — Implementer (planning):** ✅ SUCCESS. Plan at `plan/process-kb-consolidation-1.md`. 4 phases / 10 tasks / 4 batches (A: 6 parallel — DB re-index under canonical + 5 doc changes; T2 sequential — delete unscoped project after re-index verify; B: 2 parallel — full suite + new spec test; T10 sequential — memory bank + plan complete). Consolidation approach: RE-INDEX FROM SOURCE → DELETE UNScoped (learned-knowledge.instructions.md is source of truth; `knowledgebase_index` under `@abarcenas/ai-workflow-template` is idempotent/guaranteed correct; then delete `ai-workflow-template` project row — `ON DELETE CASCADE` FK cleans 16 duplicate chunks; zero-risk ordering: delete never reached if re-index fails). Alternatives rejected: UPDATE-in-place (would violate uq_knowledge_chunk unique constraint), content-level merge (complex, no benefit), delete-first (data-loss risk). Standardization points found: tracker.agent.md:29 (no projectId guidance), knowledge-retrieval.instructions.md (no canonical rule), knowledgebase.instructions.md:27 (projectId listed "optional" → should be required), 3 orchestrator templates (:20/:25 projectName from user request → prefer package.json name), no restart note section. Already correct: knowledgebase-cli.js resolveProjectId() reads package.json name; mcp server is projectId-agnostic. Canonical rule: projectId SHALL = `require('./package.json').name`. Tests: TEST-01/02 store state (4 projects, canonical ≥16 chunks, unscoped absent), TEST-03 full suite 273/273 + setup 104/104, TEST-04-09 new `tests/spec-kb-consolidation.test.js` (5 grep-based spec-wiring tests). Risks mitigated (source change idempotency, pre-delete COUNT verify, test wording). 1 new test file + 6 modified files. ⏸️ **Confirmation gate PASSED by user.**

**Step 2 — Coder (Batch A):** LAUNCHING 6 parallel coder tasks — T1 (DB re-index canonical), T3 (tracker projectId), T4 (protocol canonical rule), T5 (KB instructions required + restart note — also absorbed plan T7 restart-note content, same file), T6 (main orchestrator), T7 (feature-pipeline + TDD orchestrators; re-batched to own the 2 pipeline orchestrators while T6 owns main).

**Step 2 — Coder (Batch A):** ✅ SUCCESS 6/6. T1: `knowledgebase_index` MCP with canonical `@abarcenas/ai-workflow-template` → "Indexed 1 chunks, updated 15, skipped 0" (idempotent upsert; canonical now 16 chunks; unscoped duplicate left intact for T2). T3: tracker.agent.md +line 30 — projectId SHALL = `require('./package.json').name`, NOT from projectName. T4: knowledge-retrieval.instructions.md +12L "### Canonical projectId" (index MUST use package.json name; search optional/match-all, same derivation if passed). T5: knowledgebase.instructions.md — line 27 `knowledgebase_index` projectId optional→required (from package.json name); +lines 192-198 "### MCP Server Restart After Configuration Changes" (restart note; threshold-default gotcha). T6: orchestrator.agent.md:25 projectName → "(extracted from user request (prefer package.json "name" for the canonical scoped projectId))". T7: feature-pipeline.agent.md:20 + tdd-orchestrator.agent.md:20 same wording (string-identical across all 3). Verified: 14/14 spec-wiring tests pass after changes; full suite 273/273 (T5 re-ran); YAML/fences intact; scope discipline held (T5 absorbed T7 restart-note to avoid parallel write on same file).

**Step 2 — Coder (T2, sequential after T1):** LAUNCHING deletion of unscoped duplicate project `ai-workflow-template` (depends on T1 re-index success).

**Step 2 — Coder (T2):** ✅ SUCCESS. Pre-delete guards passed (canonical ≥16 chunks; unscoped exists with exactly 16; no other project shares id). CLI has no delete command (only sync|search|list|stats) → used transactional parameterized SQL DELETE (temp script in OS temp dir, NOT in repo): BEGIN → verify → `DELETE FROM projects WHERE id='ai-workflow-template'` (rowCount 1; ON DELETE CASCADE removed 16 chunks) → verify 0 chunks remain + totals (4 projects / 37 chunks) → COMMIT (no guards triggered). Post-delete: `ai-workflow-template` ABSENT, canonical still 16, math verified 16+12+3+6=37. Zero data loss (source of truth = learned-knowledge.instructions.md).

**Step 2 — Coder (Batch B):** LAUNCHING 2 parallel coder tasks — T8 (full test suite + setup suite re-run), T9 (new `tests/spec-kb-consolidation.test.js` — 5 grep-based spec-wiring tests).

**Step 2 — Coder (Batch B):** ✅ SUCCESS 2/2. T8: full Vitest suite 14 files / 273 / 0 (exact match) + setup sub-suite 6 files / 104 / 0. No regressions from Batch A doc changes. Noted: package.json `test` script is actually Playwright (plan row said npm test); correct Vitest command is `npx vitest run`. T9: `tests/spec-kb-consolidation.test.js` created (164L, 11 it() blocks — TEST-05 tracker projectId derivation, TEST-06 canonical projectId heading, TEST-07 knowledgebase_index projectId required row-scoped, TEST-08 all 3 orchestrators projectName mention package.json, TEST-09 restart note + MANDATORY TO ATTEMPT regression guard). Windowed/row-scoped matching avoids false failures. Targeted 11/11; full suite now 15 files / 284 / 0 (273 + 11). Phase 3 COMPLETE.

**Step 2 — Coder (T10, sequential):** LAUNCHING final step — mark plan Completed + final memory-bank consolidation.

**Step 2 — Coder (T10):** ✅ SUCCESS. Plan frontmatter status → Completed + intro badge + Phase 4 header; memory-bank activeContext/progress/tasks/_index consolidated; memory_update ran (1985 vectors). KB verified: 4 projects / 37 chunks, duplicate absent, canonical 16 chunks (16+12+3+6=37). Plan fully complete (10/10 tasks, 4 phases).

**Step 2 — Coder (combined):** ✅ SUCCESS 10/10 tasks. Data: canonical re-indexed (16 chunks), duplicate project deleted (transactional SQL, cascade), store 4 projects / 37 chunks. Standardization: projectId SHALL = `require('./package.json').name` enforced in tracker spec, protocol file, knowledgebase instructions (projectId required), 3 orchestrator templates; restart note documented. New test `tests/spec-kb-consolidation.test.js` (11 assertions). Full suite 15 files / 284 / 0 + setup 104/104.

**Step 3 — Unit-tester (independent validation):** LAUNCHING full-suite re-run + store-state + standardization verification.

**Step 3 — Unit-tester:** ✅ SUCCESS — PASS, NO ISSUES. New spec test 11/11; spec-wiring trio 25/25 (6+8+11); full suite 15 files / 284 / 0 exact match. Independent spot-checks: store state (4 projects / 37 chunks; `ai-workflow-template` ABSENT; canonical 16; math 16+12+3+6=37); standardization grep-verified (tracker.agent.md:30 package.json-name guidance; knowledge-retrieval.instructions.md:51 ### Canonical projectId; knowledgebase.instructions.md:27 projectId required (search keeps optional) + :192 restart-note heading; all 3 orchestrators' projectName mention package.json); graceful-failure contract intact (MANDATORY TO ATTEMPT verbatim; hard-dependency grep 0 hits in .opencode/agents/). Coverage global 48.12% — bit-for-bit identical to pre-pipeline baseline (docs-only pipeline, 0 scripts changed). No issues.

**Step 4 — Reviewer:** LAUNCHING code review of KB consolidation + standardization.

**Step 4 — Reviewer:** ✅ **APPROVED** — no critical, no blocking major. Independent checks: live KB state 4 projects/37 chunks, duplicate absent, canonical 16; source-of-truth parity (16 `## Session:` entries = 16 chunks, zero data loss); spec test 11/11 + full suite 15 files/284/0; `scripts/` zero diff; no hardcoded unscoped projectId (grep 0 hits); no hard-dependency language. Deletion safety positive: temp script parameterized/transactional/guarded, outside repo, no repo artifacts. Standardization consistent across 5 touchpoints + cli/server already correct; search-side asymmetry documented; graceful-failure contract intact; memory-bank hygiene clean. 🟡 Major (PRE-EXISTING, not from this pipeline): `.env` tracked in git with live DATABASE_URL credential (added d788f68, no .gitignore) — remediation documented in learned-knowledge (rotate + `git rm --cached .env` + gitignore + purge history); track as follow-up, not shipped in this commit. 🔵 Minors: TEST-05 lacks negative assertion against hardcoded `projectId: 'ai-workflow-template'` (recommended hardening); orchestrator-log line-count doc inaccuracy (164L vs actual 226L); standardization changes uncommitted (commit after review); TEST-05 700-char window mildly brittle (optional). Recommendation: APPROVE.

**Step 5 — Tracker (documentation):** LAUNCHING documentation recording + lessons learned.

**Step 5 — Tracker:** ✅ SUCCESS. Full Pipeline 9 entry appended to `docs/tracker-log.md` (execution table, 13-file table, key decisions incl. fail-safe re-index-then-delete ordering + canonical projectId rule, verification results). `memory-bank/progress.md` summary added. `docs/TRACKER-INDEX.md` updated. Learned-knowledge Session 2026-08-03 appended (10 reusable knowledge items + 5 agent tuning notes: data-consolidation safety pattern, canonical projectId derivation rule, required-vs-optional asymmetry, transactional-parameterized-SQL cleanup pattern, restart-required gotcha, negative-assertion hardening, pre-existing .env credential issue). **Step 7 (persist lessons) COVERED by tracker.** Tracker exercised the new projectId guidance end-to-end: re-indexed the new session using canonical `@abarcenas/ai-workflow-template` → "Indexed 1 chunks, updated 16, skipped 0" (live confirmation consolidation holds). knowledgebase_search executed (5 results). Memory bank synced (2062 vectors).

### FINAL STATUS (Pipeline 9): ✅ COMPLETE — all steps succeeded. Full suite 15 files / 284 / 0 + setup 104/104; reviewer APPROVED. Outcome: KB consolidated (4 projects / 37 chunks, canonical `@abarcenas/ai-workflow-template` = 16 chunks, duplicate `ai-workflow-template` deleted with zero data loss); projectId convention standardized (projectId for knowledgebase_index SHALL = `require('./package.json').name`) enforced across tracker spec, protocol file, knowledgebase instructions (required), and all 3 orchestrator templates; MCP restart note documented; new spec test (11 assertions) guards the convention. Follow-ups flagged (pre-existing, not shipped): `.env` tracked with live DATABASE_URL credential (rotate + untrack + gitignore + purge history); TEST-05 negative-assertion hardening recommended; standardization changes uncommitted (commit after review).

---

## Pipeline 10: Follow-ups — .env credential remediation + TEST-05 hardening + commit Pipelines 7-9 work

- **Date:** Mon Aug 03 2026
- **Project:** ai-workflow-template
- **Base path:** /Users/aldrichallenbarcenas/develop/ai-workflow-template
- **AutoConfirm:** true (user said "address all of the follow-ups" + confirmed MCP server restarted — mandatory confirmation gates still enforced)
- **Trigger:** Pipeline 9 reviewer flagged follow-ups: (1) 🔴 SECURITY — `.env` tracked in git (d788f68) with live `DATABASE_URL` credential `postgresql://***:***@192.168.31.200:5432/postgres`, no `.gitignore` entry; remediation documented in learned-knowledge (rotate + `git rm --cached .env` + gitignore + purge history if shared). (2) 🔵 TEST-05 in `tests/spec-kb-consolidation.test.js` lacks a negative assertion against hardcoded unscoped projectId. (3) 🔵 Pipelines 7-9 changes (protocol file, agent specs, orchestrators, tests) uncommitted.
- **Context note:** User confirmed opencode/MCP server RESTARTED — the threshold fix (`?? 0.1`) is now live; this pipeline's agents should observe knowledgebase_search returning real results (evidence the fix works).

### Pipeline Plan

| Step | Agent | Purpose |
|---|---|---|
| 1 | implementer ⏸️ | Plan: safe .env remediation (untrack/gitignore/history-purge feasibility + rotation instructions), TEST-05 hardening, commit strategy |
| 2 | coder | Apply: untrack .env + gitignore + history purge (if feasible), TEST-05 negative assertion, conventional commit |
| 3 | unit-tester | Validate: suite green, .env not tracked, no leaked references |
| 4 | reviewer | Review security remediation + commit contents |
| 5 | tracker | Record docs + persist lessons |

### Execution Log

**Step 1 — Implementer (planning):** ✅ SUCCESS. Plan at `plan/process-env-remediation-1.md` (488L, 3 tasks / 2 batches, 12 TEST-IDs). Batch A parallel: T1 (TEST-05 negative assertion — explicit flat list of 21 files across .opencode/.agents/scripts, excludes docs/plan/tests/memory-bank where `projectId: 'ai-workflow-template'` legitimately appears) + T2 (.env git remediation: .gitignore + `git rm --cached .env` preserving local file + verify .env.example placeholders). T3 sequential (commit(s), must wait for T2). Key decisions: (1) credential rotation is MANUAL — agent cannot rotate remote PG password; plan documents exact `ALTER USER` SQL + password + post-rotation .env update; until rotation the credential is exposed to anyone cloning the public GitHub repo (abarcenas29/ai-workflow-template). (2) Two-commit strategy: `fix(security):` for .env remediation + `feat:` for Pipelines 7-9 feature work — keeps security fix visible in git log. (3) `git rm --cached` preserves local .env so tooling keeps working. (4) History purge (git filter-repo) recommended but not automated — rotation is the non-optional minimum. (5) Husky version-bump guard: use `--no-verify` for doc-only commits + post-commit memory-schema validation. Testing: TEST-11 non-vacuousness (temp injected violation fails test), TEST-12 clean repo passes; full suite expected 15 files / 285 tests (284+1). Knowledgebase_search now returns REAL results (5, similarities 0.238-0.346) — confirms restart activated threshold fix. ⏸️ **Confirmation gate PASSED by user.**

**Step 2 — Coder (Batch A):** LAUNCHING 2 parallel coder tasks — T1 (TEST-05 negative assertion), T2 (.env git remediation).

**Step 2 — Coder (Batch A):** ✅ SUCCESS 2/2. T1: added negative assertion to `tests/spec-kb-consolidation.test.js` (lines 107-165, 22-file flat list across .opencode/.agents/scripts; regex `/projectId\s*[:=]\s*['"]ai-workflow-template['"]/g`; scoped name does NOT match). Deviation: excluded `learned-knowledge.instructions.md` from scan list (historical journal legitimately documents the pattern at line 375 — would false-fail TEST-12; consistent with plan exclusion rationale). Targeted 12/12; full suite 15 files / 285 / 0; non-vacuity proven (injected violation into tracker.agent.md → FAIL with offender listed → restored → PASS). T2: `.gitignore` +3 lines (# Environment files + `.env` at line 18); `git rm --cached .env` (index clean, local 1073B file preserved); `.env.example` already has commented placeholder (no change); leaked-pattern grep — password 0 hits in code dirs; IP 192.168.31.200 found only in tracked historical security docs (learned-knowledge:201 + spike docs, flagged acceptable). Note: plan's own brace-glob grep had fnmatch false-negative; re-run with separate --include flags caught the 1 missed ref. Phase 1 COMPLETE.

**Step 2 — Coder (T3, sequential after T1+T2):** LAUNCHING commits — `fix(security):` for .env remediation + `feat:` for Pipelines 7-9 work.

**Step 2 — Coder (T3):** ✅ SUCCESS. Two commits: `c931cb9` fix(security): stop tracking .env with live credentials (2 files: .gitignore +3L, D .env 30L; body = manual rotation SQL + update .env + git filter-repo purge note) and `bd5f780` feat: wire agents to central knowledge vector and standardize KB projectId (29 files: protocol file, knowledgebase + learned-knowledge instructions, 10 agent specs, 3 orchestrators, 3 spec tests, vitest.config.ts, docs logs, memory-bank, 2 Pipelines 7-9 plan files). Security overrides (documented deviations): (1) `plan/process-env-remediation-1.md` left UNTRACKED — contains the real leaked password 3×; committing would re-leak into new history (overrides ALT-03). (2) REDACTED the raw password that T1/T2 siblings had written into memory-bank activeContext/progress/tasks/_index before staging (would have shipped secret in Commit 2). Verified: git ls-files .env empty; git diff HEAD~2 shows .env only as D; 0 password hits in both new commit trees (git grep). Post-commit validation: validate-memory-schema exit 0; vocab-sync exit 0 (7 tags added pre-commit, idempotent post); `--no-verify` used (doc-only, avoids undesired bump-version), manual re-validation per plan; full suite 15 files / 285 / 0. Working tree clean except untracked secret-bearing plan file. Post-commit hook auto-synced KB ("updated 17 chunks"). Follow-up: manual password rotation + history purge still needed.

**Step 3 — Unit-tester (independent validation):** LAUNCHING full-suite re-run + git-state + security verification.

**Step 3 — Unit-tester:** ✅ SUCCESS — PASS, NO ISSUES. Full suite 15 files / 285 / 0 exact match; spec trio 3 files / 26 / 0; TEST-05 negative assertion exists (22-file flat list, regex) + non-vacuity re-proven independently (temp injection → FAIL → git checkout restore → 12/12 pass). Git state: log order bd5f780 feat ← c931cb9 fix(security) ← a9a5b7d (correct); `git ls-files .env` EMPTY; commit 1 = .gitignore +3L + D .env (exact); commit 2 = 29 files (2280+/51-); `.env` absent from both trees (only .env.example in commit 2); status = untracked plan + ` M docs/.orchestrator-log.md` (orchestrator's own in-progress doc — benign). Security: password prefix 0 hits in both commits (git grep exit 1); commit-2 memory-bank files ship REDACTED (<REDACTED>/9sso… truncated); IP 192.168.31.200 only in historical docs (acceptable); .gitignore line 18 = .env. Local .env preserved on disk. Notes: local `memory-bank/.index/memory.db` binary retains pre-redaction password prefix (git-ignored, NOT a repo/commit leak; optional memory_rebuild hygiene); manual steps remain user's responsibility (rotation + history purge).

**Step 4 — Reviewer:** LAUNCHING code review of .env remediation + commits.

**Step 4 — Reviewer:** ✅ **APPROVED (with conditions)**. Security remediation verified sound: .gitignore:16-18 has .env; git ls-files .env empty; local .env preserved (1073B); password 0 hits in HEAD + HEAD~1 (only pre-existing HEAD~2:.env); fix commit scope exact (D .env + M .gitignore only). Redaction complete (memory-bank ships <REDACTED>/truncated; 45/45 tags known; no doc corruption). TEST-05 negative assertion meaningful + non-vacuous (proven twice); blind-spot scan 0 matches outside exclusions; minors: static flat list, unquoted-YAML form unmatched (theoretical). Commit hygiene: conventional style, fix→feat order, correct groupings, --no-verify justified + post-commit validation sound. Pipelines 7-9 integrity intact; full suite 285/285 re-run. Risk confirmed: leak origin d788f68 on origin (public GitHub history); new commits clean + local-only on feat/utilize-pg-vector-db. 🟡 Major (condition #1): untracked `plan/process-env-remediation-1.md` holds the live password (lines 52/160/172) — re-leak footgun for git add -A; fix = redact 3 occurrences → <REDACTED>, verify 0 hits, commit or delete. 🔵 Minors: static flat list (RISK-06 documented); unquoted-YAML form (theoretical); memory_rebuild alone insufficient — needed VACUUM (reviewer actioned: index residue purged, 0 hits verified). Condition #2 (manual): user must rotate credential (non-optional) + decide on history purge.

**Step 4b — Coder (review fix, condition #1):** LAUNCHING redaction of plan/process-env-remediation-1.md + commit.

**Step 4b — Coder:** ✅ SUCCESS. 3 password occurrences redacted → `<REDACTED>` (lines 52/160/172; docs/TEST-IDs/instructions preserved; kept private LAN IP + `postgres` user — not secrets, no credential accompanies); verified `grep -c` 0, `git grep HEAD` 0 hits, 0 untracked files, validate-memory-schema exit 0. Commit `efc3cf5` `docs: add env remediation plan (secrets redacted)` (488 insertions, 1 file, --no-verify doc-only). Prior commits untouched.

**Step 3b — Reviewer (final sign-off):** LAUNCHING re-review of condition #1 resolution.

**Step 3b — Reviewer:** ⚠️ CHANGES REQUESTED (narrow, single fix C1). Condition #1 fully resolved at HEAD: plan file tracked + committed `efc3cf5` (redacted, 0 hits, useful); commit order c931cb9→bd5f780→efc3cf5 correct; .env untracked; suite 285/285; manual rotation/purge documented. **C1 (new):** the pipeline's own uncommitted memory-bank working-tree changes (unit-tester/reviewer appended summaries) re-introduce the FULL password verbatim in 3 files — activeContext.md:887, progress.md:2306, tasks/_index.md:17 (quoting validation report text). HEAD is clean (0 hits), but `git add -A` would commit the secret — same footgun class. Reviewer deliberately did NOT append memory-bank entries to avoid entrenching the leak. Fix: redact 3 lines to <REDACTED>/9sso… convention, verify 0 working-tree hits; then re-review clears to APPROVED. 🔵 Minors: local .index/memory.db-wal holds the value (gitignored; rebuild after C1); pre-existing history refs (user-side purge).

**Step 4c — Coder (C1 redaction):** LAUNCHING redaction of 3 memory-bank working-tree occurrences.

**Step 4c — Coder:** ✅ SUCCESS. 4 literal occurrences across 3 tracked files redacted to `9sso…`/`<REDACTED>` convention (activeContext.md:887 grep-cmd refs, progress.md:2306, tasks/_index.md:17; context preserved). Verified: `git grep -F` working tree 0 hits; rg full 32-char value repo-wide 0 hits (excl. .env local-by-design); HEAD clean. Memory index: update ran after redaction + `memory_rebuild` purged WAL residue (0 hits in .index/; semantic search returns only truncated form). No commit made (orchestrator decides). Self-audit: own notes written strictly truncated-form.

**Step 3c — Reviewer (final confirmation):** LAUNCHING confirmation that C1 is resolved.

**Step 3c — Reviewer:** ✅ **APPROVED — C1 resolved, no further conditions.** Independent verification: working-tree prefix grep 0 hits; full 32-char value 0 hits; 3 memory-bank diffs use truncated/redacted forms only (0 full-prefix occurrences, context preserved); `.index/` (memory.db/-wal/-shm) 0 hits post-rebuild; `git status` exactly 4 expected files (3 memory-bank + orchestrator log); full suite 15 files / 285 / 0; `git ls-files .env` empty; validate-memory-schema exit 0. 🔵 Informational: 5-char `9sso…` traceability prefix retained (cannot reconstruct secret; consistent with accepted convention); manual user steps (rotation + history purge) remain user-executed, documented.

**Step 5 — Tracker (documentation):** LAUNCHING documentation recording + lessons learned.

**Step 5 — Tracker:** ✅ SUCCESS. Full Pipeline 10 entry appended to `docs/tracker-log.md` (9-row execution table, 8-file table, 11 key decisions, 13 verification results, follow-up). `memory-bank/progress.md` summary (REDACTION-SAFE). `docs/TRACKER-INDEX.md` updated. Learned-knowledge Session 2026-08-03 appended (18th session; 8 reusable items + 7 agent tuning notes: tracked-secret remediation pattern, redact-all-sinks incl. WAL/VACUUM, untracked-secret footgun, negative-assertion hardening, reviewers must not append to dirty memory-bank). **Step 7 (persist lessons) COVERED by tracker.** Re-indexed under canonical projectId → "Indexed 1 chunks, updated 17, skipped 0" (registerProject fix re-confirmed). knowledgebase_search executed — 5 results (similarities 0.31-0.42 — live evidence restart activated threshold fix). 0 hits of leaked value in all written files (only <REDACTED>/9sso… forms).

### FINAL STATUS (Pipeline 10): ✅ COMPLETE — all steps succeeded. Full suite 15 files / 285 / 0; reviewer APPROVED (after condition #1 + C1 fix passes, no further conditions). Outcome: (1) 🔴 `.env` credential remediated at git level — 3 commits: `c931cb9` fix(security) stop tracking .env, `bd5f780` feat wire agents to knowledge vector + standardize KB projectId, `efc3cf5` docs env remediation plan (secrets redacted); leaked password 0 hits across working tree, HEAD, git index, and memory vector index (incl. WAL); local `.env` preserved. (2) 🔵 TEST-05 negative assertion added (non-vacuity proven twice; suite 285/285). (3) Pipelines 7-9 work committed. MANUAL USER STEPS REMAINING: rotate the Postgres credential (ALTER USER + update .env) — non-optional (credential still in public history d788f68 on origin) — and decide on history purge (git filter-repo). Working tree: only orchestrator-log + memory-bank + tracker-log doc changes uncommitted (this pipeline's own record).
